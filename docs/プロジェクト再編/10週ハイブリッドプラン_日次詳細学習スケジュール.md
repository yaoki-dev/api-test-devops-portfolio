# 10週最適化プラン - 日次詳細学習スケジュール

*最終更新: 2025年10月09日*

## プラン概要

### 基本情報
- **学習期間**: 9週 (432時間)
- **成功確率**: 85-90%（9週最適化版） ※Docker実務経験6ヶ月により上方修正
- **到達時給**: 3,800-4,200円 ※システムテスト1.5年+マルチ言語経験を再評価

### プラン構成
```
Phase 1 (Week 1-5): Foundation - 210時間
├─ Week 1 Day1-6: Python + httpx Core (42h)
├─ Week 2 Day7-12: Async基礎 (42h)
├─ Week 3 Day13-18: Async/Await深化 + 並行処理 (42h)
├─ Week 4 Day19-24: Error Handling深化 (42h)
└─ Week 5 Day25-30: pytest + Pydantic Settings (42h)

Phase 2 (Week 6-8): DevOps Integration - 126時間
├─ Week 6 Day31-36: 統合復習 + 自律チャレンジ ⭐ (42h)
├─ Week 7 Day37-42: Docker基盤構築 (42h)
└─ Week 8 Day43-48: CI/CD統合 (42h)

Phase 3 (Week 9-10): Market Ready - 120時間
├─ Week 9 Day49-54: ポートフォリオ最適化 (60h)
└─ Week 10 Day55-60: 応募準備 + 初回応募 (60h)
```

--

## Phase 1: Foundation (Week 1-5, 210時間)

### Week 1: Python + httpx Core (42時間)

**学習目標**:
- Python基礎文法の実装レベル理解
- httpx基本操作の習熟
- エラーハンドリング基礎パターン
- 15テスト作成・実行能力

**認知負荷**: 75%
**自律目標**: 基礎理解30%、実装自律20%

**学習フロー**: [CLAUDE.md#AI協働学習フロー仕組み]
- Phase 1: AI説明・概念理解（目標理解度30%）
- Phase 2: AI協働実装（目標理解度60%）
- Phase 3: 理解度確認・記録（目標理解度70%+）

---

#### **Day 1 (月曜): Python基礎復習 + httpx導入**

### 📋 学習サマリー
- **学習内容**:
  - Python基礎復習: 型ヒント、Context Manager、例外処理
  - httpx基礎: httpx.Client、GET/POST、Response handling
  - pytest基礎: テスト作成パターン、assert、基本テストケース設計
- **ポートフォリオ成果物**:
  - [ ] 型ヒント付き関数3個作成
  - [ ] Context Manager実装1個
  - [ ] 例外処理パターン3種類理解
  - [ ] BaseAPIClientクラス雛形作成
  - [ ] GET/POSTメソッド実装
  - [ ] 基本エラー処理追加
  - [ ] tests/unit/test_api_client.py作成
  - [ ] 基本テスト5件作成
  - [ ] pytest実行成功

**総学習時間**: 7時間
**カバレッジ目標**: 6.6%

**Phase 1: AI説明・概念理解 (2.5h)**
- Python基礎復習: 型ヒント、Context Manager、例外処理 **[50分]**
- httpx基礎概念: httpx.Client、GET/POST、Response handling **[50分]**
- pytest基礎概念: テスト作成パターン、assert、基本テストケース設計 **[40分]**
- 理解度測定（15分） **[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] 型ヒント（Type Hints）の役割と基本的な使い方を説明できる
  - [ ] Context Manager（with文）の動作原理とリソース管理を理解している
  - [ ] httpx.Clientの基本的な使い方（GET/POST）を理解している
  - [ ] pytestの基本的なテスト作成パターンとassertの使い方を理解している
  - [ ] 例外処理の階層設計（try/except）の重要性を説明できる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- Python基礎強化（型アノテーション、with文、try/except階層）実装 **[70分]**
- httpx同期クライアント基礎習得（GET/POSTメソッド、エラー処理）実装 **[60分]**
- pytest基礎とテスト作成（基本テストケース5件作成、pytest実行確認） **[30分]**
- バッファ **[20分]**

#### AI依頼プロンプト
```
「{学習内容}に基づき、以下の実装を完成させてください。

実装タスク:
{実装タスク}

要件:
1. 技術要件: {学習内容}で学習した技術パターンを適用
2. 品質基準: pytest合格、カバレッジ{カバレッジ目標}%達成、ruff/mypy合格
3. セキュリティ: 入力検証、例外処理、型安全性確保
4. パフォーマンス: 計算量O(n)以下、メモリ効率考慮

成果物:
{ポートフォリオ成果物}のチェックリスト項目を満たす実装

制約:
- PEP 8準拠、Python 3.10+互換性維持
- 既存コード（utils/api_client.py、config/settings.py）との整合性確保
```

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- つまずき箇所の復習・質問解決

---

**実装要件**:
- Python基礎強化（型アノテーション、with文、try/except階層）
- httpx同期クライアント基礎習得（GET/POSTメソッド、エラー処理）
- pytest基礎とテスト作成パターン習得（pytest実行、基本テストケース）

**実装タスク**:
```python
# AI支援で作成 (utils/api_client.py)
class BaseAPIClient:
    def __init__(self, base_url: str):
        self.base_url = base_url
        self.client = httpx.Client()

    def get(self, endpoint: str) -> dict:
        # AI生成: 80%
        pass

    def post(self, endpoint: str, data: dict) -> dict:
        # AI生成: 80%
        pass
```

---

#### **Day 2 (火曜): エラー階層設計 + リトライロジック**

### 📋 学習サマリー
- **学習内容**:
  - エラー階層設計: 基底例外定義、HTTP例外設計、BaseAPIClientへの統合
  - リトライメカニズム: リトライデコレータ理解、Exponential backoff実装
  - エラーケーステスト: エラーテスト作成、カバレッジ測定（目標13.2%以上）
- **ポートフォリオ成果物**:
  - [ ] exceptions.py作成 (5例外クラス)
  - [ ] BaseAPIClientに統合
  - [ ] エラー階層図作成 (手書き/mermaid)
  - [ ] リトライメソッド実装
  - [ ] Exponential backoff実装
  - [ ] リトライ回数設定可能化
  - [ ] エラーテスト10件追加
  - [ ] 累計15テスト
  - [ ] カバレッジ60%以上

**総学習時間**: 7時間
**カバレッジ目標**: 13.2%（累積）

**Phase 1: AI説明・概念理解 (2.5h)**
- エラー階層設計概念: 基底例外定義、HTTP例外設計、例外継承構造 **[50分]**
- リトライメカニズム概念: リトライデコレータ理解、Exponential backoff戦略 **[50分]**
- エラーケーステスト概念: エラーテスト設計パターン、カバレッジ戦略 **[40分]**
- 理解度測定（15分） **[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] エラー階層設計の重要性（保守性・拡張性向上）と継承構造（基底例外→特化例外）を説明できる
  - [ ] リトライメカニズムの動作原理（試行回数、遅延戦略）とネットワーク障害対策の必要性を理解している
  - [ ] Exponential backoffの概念（指数的遅延増加）とサーバー負荷軽減効果を説明できる
  - [ ] 4xxエラー（クライアントエラー）と5xxエラー（サーバーエラー）の処理方針の違いとリトライ可否判断基準を説明できる
  - [ ] エラーケーステスト設計パターン（正常系・異常系・境界値）とバグ早期発見の重要性を理解している

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- エラー階層実装（exceptions.py作成、BaseAPIClientへの統合） **[70分]**
- リトライメカニズム実装（Exponential backoff、リトライ回数設定） **[60分]**
- エラーケーステスト実装（10件追加、カバレッジ測定） **[30分]**
- バッファ **[20分]**

#### AI依頼プロンプト
```
「{学習内容}に基づき、以下の実装を完成させてください。

実装タスク:
{実装タスク}

要件:
1. 技術要件: {学習内容}で学習した技術パターンを適用
2. 品質基準: pytest合格、カバレッジ{カバレッジ目標}%達成、ruff/mypy合格
3. セキュリティ: 入力検証、例外処理、型安全性確保
4. パフォーマンス: 計算量O(n)以下、メモリ効率考慮

成果物:
{ポートフォリオ成果物}のチェックリスト項目を満たす実装

制約:
- PEP 8準拠、Python 3.10+互換性維持
- 既存コード（utils/api_client.py、config/settings.py）との整合性確保
```

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- つまずき箇所の復習・質問解決

---

**実装タスク**:
```python
# エラー階層設計 (AI 75%)
class APIClientError(Exception):
    """基底例外クラス"""
    pass

class APIConnectionError(APIClientError):
    """接続エラー"""
    pass

class APIHTTPError(APIClientError):
    def __init__(self, status_code: int, message: str):
        # AI実装 → 引数設計理解

class APITimeoutError(APIClientError):
    # AI生成

# リトライメカニズム (AI 80%)
def _retry_request(self, method: str, endpoint: str, retries: int = 3):
    for attempt in range(retries):
        try:
            # AI生成: リトライロジック80%
            # Exponential backoff: delay = base_delay * (2 ** attempt)
            pass
        except APIConnectionError:
            if attempt == retries - 1:
                raise
            time.sleep(delay)

# エラーケーステスト (AI 85%)
# - test_connection_error_retry
# - test_timeout_error
# - test_http_4xx_no_retry
# - test_http_5xx_retry
# - test_max_retries_exceeded
```

---

#### **Day 3 (水曜): JSONPlaceholder統合開始**

### 📋 学習サマリー
- **学習内容**:
  - JSONPlaceholder API仕様理解
  - 専用クライアント設計概念（継承、エンドポイント設計）
  - 統合テスト設計パターン
- **ポートフォリオ成果物**:
  - [ ] JSONPlaceholderClientクラス雛形作成
  - [ ] get_user実装
  - [ ] get_posts実装開始
  - [ ] 統合テスト2件作成
  - [ ] pytest -m integration実行成功

**総学習時間**: 7時間
**カバレッジ目標**: 19.8%（累積）

**Phase 1: AI説明・概念理解 (2.5h)**
- JSONPlaceholder API仕様理解 **[50分]**
- 専用クライアント設計概念（継承、エンドポイント設計） **[50分]**
- 統合テスト設計パターン **[40分]**
- 理解度測定（15分） **[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] JSONPlaceholder APIの主要エンドポイント（users, posts, todos）の役割と使い方を説明できる
  - [ ] BaseAPIClient継承によるコード再利用のメリットと実装パターンを理解している
  - [ ] クラス継承とメソッドオーバーライドの基本概念を説明できる
  - [ ] RESTful APIのエンドポイント設計原則（リソース指向、HTTP動詞）を理解している
  - [ ] 統合テストの基本設計パターン（実API呼び出し、レスポンス検証）を理解している

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- JSONPlaceholderClient実装（BaseAPIClient継承、get_user, get_posts実装） **[70分]**
- 統合テスト作成（2件、基本テストパターン） **[60分]**
- テスト実行・デバッグ **[30分]**
- バッファ **[20分]**

#### AI依頼プロンプト
```
「{学習内容}に基づき、以下の実装を完成させてください。

実装タスク:
{実装タスク}

要件:
1. 技術要件: {学習内容}で学習した技術パターンを適用
2. 品質基準: pytest合格、カバレッジ{カバレッジ目標}%達成、ruff/mypy合格
3. セキュリティ: 入力検証、例外処理、型安全性確保
4. パフォーマンス: 計算量O(n)以下、メモリ効率考慮

成果物:
{ポートフォリオ成果物}のチェックリスト項目を満たす実装

制約:
- PEP 8準拠、Python 3.10+互換性維持
- 既存コード（utils/api_client.py、config/settings.py）との整合性確保
```

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- つまずき箇所の復習・質問解決

---

**実装タスク**:
```python
# 専用クライアント作成 (AI 80%)
class JSONPlaceholderClient(BaseAPIClient):
    def __init__(self):
        super().__init__("https://jsonplaceholder.typicode.com")

    def get_user(self, user_id: int) -> dict:
        # AI生成 → エンドポイント設計理解
        return self.get(f"/users/{user_id}")

    def get_posts(self, user_id: int | None = None) -> list[dict]:
        # AI生成 → クエリパラメータ理解
        pass
```

```python
# 統合テスト開始 (AI 80%)
# tests/integration/test_jsonplaceholder.py

@pytest.mark.integration
def test_get_user_success():
    client = JSONPlaceholderClient()
    user = client.get_user(1)
    assert user["id"] == 1
    # AI: 基本アサーション生成
```

**ポートフォリオ成果物**:
- [ ] JSONPlaceholderClientクラス雛形作成
- [ ] get_user実装
- [ ] get_posts実装開始
- [ ] 統合テスト2件作成
- [ ] pytest -m integration実行成功

---

#### **Day 4 (木曜): JSONPlaceholder機能拡張**

### 📋 学習サマリー
- **学習内容**:
  - POST操作設計概念（create_post データ構造設計）
  - クエリパラメータ設計概念（get_todos フィルタリング）
  - 統合テスト拡張パターン
- **ポートフォリオ成果物**:
  - [ ] create_post実装
  - [ ] get_todos実装
  - [ ] 統合テスト5件追加
  - [ ] 累計10テスト達成

**総学習時間**: 7時間
**カバレッジ目標**: 26.4%（累積）

**Phase 1: AI説明・概念理解 (2.5h)**
- POST操作設計概念（create_post データ構造設計） **[50分]**
- クエリパラメータ設計概念（get_todos フィルタリング） **[50分]**
- 統合テスト拡張パターン **[40分]**
- 理解度測定（15分） **[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] pytest Fixtureの3つのスコープ（session/module/function）の使い分け基準を説明できる
  - [ ] @pytest.mark.parametrizeによるテストデータ駆動設計のメリットを理解している
  - [ ] Mock/Stubの違いと、respxを使った外部API依存の排除方法を説明できる
  - [ ] 正常系テストと異常系テスト（エラーケース）の網羅的な設計パターンを理解している
  - [ ] pytestカバレッジレポート（--cov）の読み方と未カバー箇所の特定方法を知っている

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- create_post実装（POST操作、データ構造設計） **[70分]**
- get_todos実装（クエリパラメータ、フィルタリング） **[60分]**
- 統合テスト5件追加（POSTテスト、パラメータテスト） **[30分]**
- バッファ **[20分]**

#### AI依頼プロンプト
```
「{学習内容}に基づき、以下の実装を完成させてください。

実装タスク:
{実装タスク}

要件:
1. 技術要件: {学習内容}で学習した技術パターンを適用
2. 品質基準: pytest合格、カバレッジ{カバレッジ目標}%達成、ruff/mypy合格
3. セキュリティ: 入力検証、例外処理、型安全性確保
4. パフォーマンス: 計算量O(n)以下、メモリ効率考慮

成果物:
{ポートフォリオ成果物}のチェックリスト項目を満たす実装

制約:
- PEP 8準拠、Python 3.10+互換性維持
- 既存コード（utils/api_client.py、config/settings.py）との整合性確保
```

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- つまずき箇所の復習・質問解決

---

**実装タスク**:
```python
# JSONPlaceholderClient拡張 (AI 78%)
def create_post(self, title: str, body: str, user_id: int) -> dict:
    # AI生成 → POSTデータ構造理解
    pass

def get_todos(self, user_id: int | None = None) -> list[dict]:
    # AI生成 → 引数設計理解
    pass
```

```python
# 統合テスト拡充 (AI 80%)
@pytest.mark.integration
def test_get_posts_by_user():
    # AI生成: フィルタリングテスト
    pass

@pytest.mark.integration
def test_create_post():
    # AI生成: POST操作テスト
    pass

# 5テスト追加
```

---

#### **Day 5 (金曜): テスト充実化 + 中間確認**

### 📋 学習サマリー
- **学習内容**:
  - エラーケーステスト設計概念
  - カバレッジ測定方法・ツール理解
  - テストカバレッジ戦略
- **ポートフォリオ成果物**:
  - [ ] エラーケーステスト5件追加
  - [ ] 累計15テスト達成
  - [ ] カバレッジ33.0%以上達成
  - [ ] 全テスト合格

**総学習時間**: 7時間
**カバレッジ目標**: 33.0%（累積）

**Phase 1: AI説明・概念理解 (2.5h)**
- エラーケーステスト設計概念 **[50分]**
- カバレッジ測定方法・ツール理解 **[50分]**
- テストカバレッジ戦略 **[40分]**
- 理解度測定（15分） **[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] Pydantic SettingsとBaseSettingsの継承関係と自動バリデーション機能を説明できる
  - [ ] 環境変数の命名規則（__区切りネスト記法）とマッピングルールを理解している
  - [ ] SecretStrによる機密情報保護の仕組みと.envファイル除外の重要性を説明できる
  - [ ] Field(...)によるバリデーションルール（ge/le/regex等）の設計パターンを理解している
  - [ ] APIConfig/LogConfigのようなネスト設定クラスの設計メリットを説明できる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- エラーケーステスト5件追加（invalid_user_id, connection_error等） **[70分]**
- カバレッジ測定（pytest --cov、レポート分析） **[60分]**
- カバレッジ33.0%以上達成確認（改善実装） **[30分]**
- バッファ **[20分]**

#### AI依頼プロンプト
```
「{学習内容}に基づき、以下の実装を完成させてください。

実装タスク:
{実装タスク}

要件:
1. 技術要件: {学習内容}で学習した技術パターンを適用
2. 品質基準: pytest合格、カバレッジ{カバレッジ目標}%達成、ruff/mypy合格
3. セキュリティ: 入力検証、例外処理、型安全性確保
4. パフォーマンス: 計算量O(n)以下、メモリ効率考慮

成果物:
{ポートフォリオ成果物}のチェックリスト項目を満たす実装

制約:
- PEP 8準拠、Python 3.10+互換性維持
- 既存コード（utils/api_client.py、config/settings.py）との整合性確保
```

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- つまずき箇所の復習・質問解決

---

**実装タスク**:
```python
# エラーケーステスト追加 (AI 75%)
# - test_invalid_user_id
# - test_connection_error_handling
# - test_timeout_handling

# 5テスト追加
```

```bash
# カバレッジ測定
uv run pytest --cov=utils --cov-report=html
# 目標33.0%達成確認
```

**中間評価チェックリスト**:
- [ ] 進捗70%達成
- [ ] 理解度25%以上
- [ ] カバレッジ33.0%達成

---

#### **Day 6 (土曜): Week 1振り返り + Week 2準備**

### 📋 学習サマリー
- **学習内容**:
  - README作成概念（プロジェクト概要、セットアップ、アーキテクチャ説明）
  - コード品質管理概念（ruff、pre-commit、docstring標準）
  - Week 1振り返り手法（学習成果整理、メトリクス確認）
- **ポートフォリオ成果物**:
  - [ ] README.md完成版作成（概要・セットアップ・使用例・アーキテクチャ）
  - [ ] pre-commitフック導入（ruff自動修正）
  - [ ] docstring追加 (主要クラス3個: BaseAPIClient, JSONPlaceholderClient, Settings)
  - [ ] Week 1学習成果記録（daily_progress.md更新）
  - [ ] 全品質ゲート合格（pytest/ruff/mypy）

**総学習時間**: 7時間
**カバレッジ目標**: 39.5%（累積）

**Phase 1: AI説明・概念理解 (2.5h)**
- README作成概念（プロジェクト概要、セットアップ手順、アーキテクチャ説明） **[50分]**
- コード品質管理概念（ruff、pre-commit hooks、docstring標準） **[50分]**
- Week 1振り返り手法（学習成果整理、つまずき分析） **[30分]**
- 理解度測定（15分） **[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] httpx + pytest + Pydantic Settingsの統合アーキテクチャ全体像を説明できる
  - [ ] BaseAPIClientの責務（リトライ/エラーハンドリング/ログ記録）を理解している
  - [ ] テストカバレッジ39.5%達成のための優先テスト対象（コアロジック優先）を判断できる
  - [ ] 階層的例外設計（APIError → ConnectionError/TimeoutError）の保守性メリットを説明できる
  - [ ] Context Manager（with文）によるリソース管理とクリーンアップの動作原理を理解している
  - [ ] pre-commit hooksによる自動品質チェックの仕組みを理解している

判定基準:
  - 4つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-3つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- 実装タスク1（README作成 + pre-commit導入） **[70分]**
  - README.md完成版作成（プロジェクト概要・セットアップ・使用例・アーキテクチャ図）: 50分
  - pre-commitフック導入（pyproject.toml設定、.pre-commit-config.yaml作成）: 20分
- 実装タスク2（docstring追加） **[60分]**
  - BaseAPIClient: 20分
  - JSONPlaceholderClient: 20分
  - Settings: 20分
- 実装タスク3（Week 1振り返り + 段階的検証） **[30分]**
  - Week 1振り返り記録（daily_progress.md更新、つまずき箇所リストアップ）: 15分
  - 段階的検証（README/pre-commit/mypy実行、品質ゲート確認）: 15分
- バッファ **[20分]**

#### AI依頼プロンプト
```
「{学習内容}に基づき、以下の実装を完成させてください。

実装タスク:
{実装タスク}

要件:
1. 技術要件: {学習内容}で学習した技術パターンを適用
2. 品質基準: pytest合格、カバレッジ{カバレッジ目標}%達成、ruff/mypy合格
3. セキュリティ: 入力検証、例外処理、型安全性確保
4. パフォーマンス: 計算量O(n)以下、メモリ効率考慮

成果物:
{ポートフォリオ成果物}のチェックリスト項目を満たす実装

制約:
- PEP 8準拠、Python 3.10+互換性維持
- 既存コード（utils/api_client.py、config/settings.py）との整合性確保
```

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- Week 1総復習・弱点補強

---

### Week 2: Async基礎 (42時間)

**学習目標**:
- Async/Await基礎理解
- httpx.AsyncClient習熟
- pytest-asyncio導入
- 30テスト達成 (カバレッジ54.74%)

**認知負荷**: 75%
**自律目標**: Async理解30%、実装自律25%

**学習フロー**: @[CLAUDE.md#AI協働学習フロー仕組み]
- Phase 1: AI説明・概念理解（目標理解度30%）
- Phase 2: AI協働実装（目標理解度60%）
- Phase 3: 理解度確認・記録（目標理解度70%+）

---

#### **Day 7 (月曜): Async CRUD基礎 - GET操作**

**総学習時間**: 7時間
**カバレッジ目標**: 44.58%（累積）

**Phase 1: AI説明・概念理解 (2.5h)**
- Async/Await基礎概念（asyncio、イベントループ、async/await構文）**[50分]**
- httpx.AsyncClient概念（同期版との違い、Context Manager、並行処理基礎）**[50分]**
- 非同期エラーハンドリング概念（例外伝播、async try/except）**[40分]**
- 理解度測定（15分）**[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] イベントループ(Event Loop)の役割とコルーチン実行の仕組みを説明できる
  - [ ] async def/awaitキーワードの使い方と同期関数との違いを理解している
  - [ ] asyncio.gather()による並行処理パターンと順序保証を説明できる
  - [ ] 非同期Context Manager(__aenter__/__aexit__)の実装方法を理解している
  - [ ] AsyncAPIClientでのhttpx.AsyncClientリソース管理パターンを説明できる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- AsyncAPIClient実装（__init__, async get, async post基礎）**[70分]**
- AsyncJSONPlaceholderClient実装（async get_user, async get_posts）**[60分]**
- pytest-asyncio導入（@pytest.mark.asyncio、async test patterns）**[30分]**
- 非同期テスト4件作成 **[40分]**
- バッファ **[20分]**

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- つまずき箇所の復習・質問解決

---

**実装タスク**:
```python
# AsyncAPIClient基礎 (AI 80%)
class AsyncAPIClient:
    def __init__(self, base_url: str):
        self.base_url = base_url
        self._client = None

    async def __aenter__(self):
        self._client = httpx.AsyncClient(base_url=self.base_url)
        return self
        # AI実装 → async Context Manager理解

    async def __aexit__(self, exc_type, exc_val, exc_tb):
        if self._client:
            await self._client.aclose()
        # AI実装 → リソース解放理解

    async def get(self, endpoint: str) -> dict:
        response = await self._client.get(endpoint)
        response.raise_for_status()
        return response.json()
        # AI生成 → await動作理解
```

```python
# AsyncJSONPlaceholderClient (AI 75%)
class AsyncJSONPlaceholderClient(AsyncAPIClient):
    def __init__(self):
        super().__init__("https://jsonplaceholder.typicode.com")

    async def get_user(self, user_id: int) -> dict:
        return await self.get(f"/users/{user_id}")
        # AI生成 → エンドポイント設計理解

    async def get_posts(self, user_id: int | None = None) -> list[dict]:
        endpoint = "/posts"
        if user_id:
            endpoint += f"?userId={user_id}"
        return await self.get(endpoint)
        # AI生成 → クエリパラメータ理解
```

```python
# pytest-asyncio導入 (AI 80%)
import pytest

@pytest.mark.asyncio
async def test_async_get_user():
    async with AsyncJSONPlaceholderClient() as client:
        user = await client.get_user(1)
        assert user["id"] == 1
        assert "name" in user
    # AI生成 → pytest-asyncio理解

# 4非同期テスト作成:
# - test_async_get_user
# - test_async_get_posts_all
# - test_async_get_posts_by_user
# - test_async_context_manager
```

**ポートフォリオ成果物**:
- [ ] AsyncAPIClient基礎実装
- [ ] async Context Manager実装
- [ ] AsyncJSONPlaceholderClient実装
- [ ] async get_user実装
- [ ] async get_posts実装
- [ ] pytest-asyncio導入
- [ ] 非同期テスト4件作成
- [ ] 累計24テスト達成
- [ ] カバレッジ44.58%達成

---

#### **Day 8 (火曜): Async CRUD拡張 - POST/PUT/DELETE操作**

**総学習時間**: 7時間
**カバレッジ目標**: 49.66%（累積）

**Phase 1: AI説明・概念理解 (2.5h)**
- Async POST/PUT/DELETE概念（データ送信、更新、削除パターン）**[50分]**
- httpx AsyncClient POST実装（JSON送信、レスポンス処理）**[50分]**
- 非同期CRUD完結概念（Create, Read, Update, Delete全操作）**[40分]**
- 理解度測定（15分）**[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] httpx.AsyncClientの非同期HTTPメソッド(GET/POST/PUT/DELETE)の使い方を理解している
  - [ ] Connection Poolingの概念とmax_connections設定の重要性を説明できる
  - [ ] 非同期エラーハンドリング(httpx.HTTPError/TimeoutException)の実装パターンを理解している
  - [ ] AsyncJSONPlaceholderClientでの並行データ取得(asyncio.gather)実装方法を説明できる
  - [ ] 非同期処理における構造化ログ(structlog)の記録タイミングを理解している

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- AsyncAPIClient拡張（async post, async put, async delete実装）**[70分]**
- AsyncJSONPlaceholderClient CRUD完結（async create_post, async update_post, async delete_post）**[40分]**
- 非同期テスト4件作成（POST/PUT/DELETE/CRUD統合）**[50分]**
- バッファ **[20分]**

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- つまずき箇所の復習・質問解決

---

**実装タスク**:
```python
# AsyncAPIClient拡張 (AI 75%)
class AsyncAPIClient:
    async def post(self, endpoint: str, data: dict) -> dict:
        response = await self._client.post(endpoint, json=data)
        response.raise_for_status()
        return response.json()
        # AI生成 → POST処理理解

    async def put(self, endpoint: str, data: dict) -> dict:
        response = await self._client.put(endpoint, json=data)
        response.raise_for_status()
        return response.json()
        # AI生成 → PUT処理理解

    async def delete(self, endpoint: str) -> None:
        response = await self._client.delete(endpoint)
        response.raise_for_status()
        # AI生成 → DELETE処理理解
```

```python
# AsyncJSONPlaceholderClient CRUD拡張 (AI 75%)
class AsyncJSONPlaceholderClient(AsyncAPIClient):
    async def create_post(self, title: str, body: str, user_id: int) -> dict:
        data = {"title": title, "body": body, "userId": user_id}
        return await self.post("/posts", data)
        # AI生成 → create操作理解

    async def update_post(self, post_id: int, title: str, body: str) -> dict:
        data = {"id": post_id, "title": title, "body": body}
        return await self.put(f"/posts/{post_id}", data)
        # AI生成 → update操作理解

    async def delete_post(self, post_id: int) -> None:
        await self.delete(f"/posts/{post_id}")
        # AI生成 → delete操作理解
```

```python
# 非同期CRUD統合テスト (AI 80%)
@pytest.mark.asyncio
async def test_async_create_post():
    async with AsyncJSONPlaceholderClient() as client:
        post = await client.create_post(
            title="Test Post",
            body="Test Body",
            user_id=1
        )
        assert "id" in post
        assert post["title"] == "Test Post"
    # AI生成 → create test理解

# 4非同期テスト作成:
# - test_async_create_post
# - test_async_update_post
# - test_async_delete_post
# - test_async_crud_integration
```

**ポートフォリオ成果物**:
- [ ] async post実装
- [ ] async put実装
- [ ] async delete実装
- [ ] async create_post実装
- [ ] async update_post実装
- [ ] async delete_post実装
- [ ] 非同期CRUDテスト4件作成
- [ ] 累計28テスト達成
- [ ] カバレッジ49.66%達成

---

#### **Day 9 (水曜): Async/Await深化 - エラーハンドリング強化**

**総学習時間**: 7時間
**カバレッジ目標**: 52.02%（累積）

**Phase 1: AI説明・概念理解 (2.5h)**
- 非同期エラーハンドリング深化（async try/except、エラー伝播、タイムアウト処理）**[50分]**
- Async Context Manager詳細（__aenter__/__aexit__、リソース管理、例外処理）**[50分]**
- 非同期リトライロジック概念（async sleep、retry装飾パターン）**[40分]**
- 理解度測定（15分）**[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] @pytest.mark.asyncioマーカーの役割とevent_loop_policy設定を説明できる
  - [ ] event_loopフィクスチャのスコープ(session/module/function)の使い分けを理解している
  - [ ] 非同期テストパターン(await client.request()のアサーション)を実装できる
  - [ ] respxによる非同期モック(async mock)の設定方法とレスポンス制御を理解している
  - [ ] 並行処理テスト(asyncio.gather()の結果検証)の実装パターンを説明できる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- AsyncAPIClientエラーハンドリング強化（async try/except統合、タイムアウト設定）**[60分]**
- Async Context Manager改善（リソース解放保証、例外時の処理）**[50分]**
- 非同期テスト4件作成（エラーハンドリング、タイムアウト、Context Manager、リトライ）**[50分]**
- バッファ **[20分]**

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- つまずき箇所の復習・質問解決

---

**実装タスク**:
```python
# 非同期エラーハンドリング (AI 75%)
class AsyncAPIClient:
    async def get(self, endpoint: str, timeout: float = 30.0) -> dict:
        try:
            response = await self._client.get(endpoint, timeout=timeout)
            response.raise_for_status()
            return response.json()
        except httpx.TimeoutException:
            raise APITimeoutError(f"Request timeout: {endpoint}")
        except httpx.HTTPStatusError as e:
            raise APIHTTPError(e.response.status_code, str(e))
        # AI生成 → 非同期例外処理理解
```

```python
# Async Context Manager改善 (AI 75%)
class AsyncJSONPlaceholderClient(AsyncAPIClient):
    async def __aenter__(self):
        self._client = httpx.AsyncClient(
            base_url=self.base_url,
            timeout=30.0
        )
        return self
        # AI実装 → Context Manager理解

    async def __aexit__(self, exc_type, exc_val, exc_tb):
        if self._client:
            await self._client.aclose()
        # 例外が発生してもリソース解放を保証
        # AI実装 → リソース管理理解
```

```python
# 非同期テスト (AI 80%)
@pytest.mark.asyncio
async def test_async_timeout_handling():
    async with AsyncJSONPlaceholderClient() as client:
        with pytest.raises(APITimeoutError):
            await client.get("/users/1", timeout=0.001)
    # AI生成 → timeout test理解

# 4非同期テスト作成:
# - test_async_error_handling
# - test_async_timeout_handling
# - test_async_context_manager_exception
# - test_async_retry_logic
```

**ポートフォリオ成果物**:
- [ ] 非同期エラーハンドリング実装
- [ ] asyncタイムアウト処理実装
- [ ] Async Context Manager改善
- [ ] リソース解放保証実装
- [ ] 非同期例外テスト4件作成
- [ ] 累計32テスト達成
- [ ] カバレッジ52.02%達成

---

#### **Day 10 (木曜): pytest-asyncio Fixture深化 + Test Template作成**

**総学習時間**: 7時間
**カバレッジ目標**: 54.38%（累積）

**Phase 1: AI説明・概念理解 (2.5h)**
- pytest-asyncio fixture概念（async fixture作成、scope管理、依存関係）**[50分]**
- Test Template パターン（再利用可能なテストパターン、DRY原則適用）**[50分]**
- Async Test Best Practices（並行テスト設計、テスト分離）**[40分]**
- 理解度測定（15分）**[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] asyncio.CancelledErrorとasyncio.TimeoutErrorの発生条件と処理方法を説明できる
  - [ ] httpx.TimeoutExceptionとasyncio.TimeoutErrorの違いを理解している
  - [ ] 並行処理エラー集約パターン(asyncio.gather return_exceptions=True)を実装できる
  - [ ] タイムアウト階層設計(connect/read/pool timeout)の最適値設定を判断できる
  - [ ] 非同期リトライロジック(asyncio.sleep+再試行)の実装パターンを理解している

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- Async fixture実装（async_client_fixture, async_user_factory, async_post_factory）**[70分]**
- Test Template作成（CRUD操作テンプレート、エラーハンドリングテンプレート）**[60分]**
- Template活用テスト5件作成 **[30分]**
- バッファ **[20分]**

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- つまずき箇所の復習・質問解決

---

**実装タスク**:
```python
# Async Fixture作成 (AI 75%)
import pytest
from typing import AsyncGenerator

@pytest.fixture
async def async_client() -> AsyncGenerator[AsyncJSONPlaceholderClient, None]:
    """非同期クライアントフィクスチャ"""
    async with AsyncJSONPlaceholderClient() as client:
        yield client
    # AI実装 → async fixture理解

@pytest.fixture
async def async_user_factory(async_client):
    """非同期ユーザーファクトリー"""
    async def _create_user(user_id: int = 1):
        return await async_client.get_user(user_id)
    return _create_user
    # AI実装 → async factory pattern理解

@pytest.fixture
async def async_post_factory(async_client):
    """非同期投稿ファクトリー"""
    async def _create_post(title: str, body: str, user_id: int = 1):
        return await async_client.create_post(title, body, user_id)
    return _create_post
    # AI実装 → factory with parameters理解
```

```python
# Test Template作成 (AI 80%)
class AsyncCRUDTestTemplate:
    """CRUD操作テストテンプレート"""

    async def test_create_success(self, async_client):
        """作成成功テンプレート"""
        result = await async_client.create_post(
            title="Test",
            body="Body",
            user_id=1
        )
        assert "id" in result
        # AI生成 → template pattern理解

    async def test_read_success(self, async_client):
        """読取成功テンプレート"""
        pass

    async def test_update_success(self, async_client):
        """更新成功テンプレート"""
        pass

    async def test_delete_success(self, async_client):
        """削除成功テンプレート"""
        pass

    async def test_error_handling(self, async_client):
        """エラーハンドリングテンプレート"""
        with pytest.raises(APIHTTPError):
            await async_client.get_user(999999)
        # AI生成 → error test template理解

# 5非同期テスト作成:
# - test_async_fixture_scope
# - test_async_user_factory
# - test_async_post_factory
# - test_crud_template_create
# - test_crud_template_error
```

**ポートフォリオ成果物**:
- [ ] async_client fixture実装
- [ ] async_user_factory実装
- [ ] async_post_factory実装
- [ ] AsyncCRUDTestTemplate作成
- [ ] template活用テスト5件作成
- [ ] 累計37テスト達成
- [ ] カバレッジ54.38%達成

---

#### **Day 11 (金曜): Concurrent Patterns実践 - asyncio.gather()活用**

**総学習時間**: 7時間
**カバレッジ目標**: 53.50%（累積）

**Phase 1: AI説明・概念理解 (2.5h)**
- asyncio.gather()詳細（並行実行パターン、結果集約、例外処理）**[50分]**
- Concurrent Patterns（並行データ取得、バッチ処理、タイムアウト管理）**[50分]**
- パフォーマンス測定手法（時間計測、同期vs非同期比較、ボトルネック分析）**[40分]**
- 理解度測定（15分）**[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] 並行度(concurrency)とスループット(throughput)の関係を説明できる
  - [ ] I/O待機時間削減戦略(並行リクエスト vs 逐次リクエスト)を理解している
  - [ ] Connection Pool sizing理論(max_connections設定根拠)を説明できる
  - [ ] Rate Limiting設計(asyncio.Semaphore)の実装方法と同時接続数制御を理解している
  - [ ] 最適並行度判定基準(CPU/ネットワーク制約)を判断できる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- asyncio.gather()実装（複数ユーザー並行取得、エラーハンドリング統合）**[70分]**
- Concurrent Patternsテスト6件作成（並行処理、タイムアウト、エラー伝播）**[60分]**
- パフォーマンステスト実装（同期vs非同期比較、計測自動化）**[30分]**
- バッファ **[20分]**

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- つまずき箇所の復習・質問解決

---

**実装タスク**:
```python
# asyncio.gather()活用 (AI 75%)
class AsyncJSONPlaceholderClient(AsyncAPIClient):
    async def get_multiple_users(self, user_ids: list[int]) -> list[dict]:
        """複数ユーザー並行取得"""
        tasks = [self.get_user(user_id) for user_id in user_ids]
        results = await asyncio.gather(*tasks, return_exceptions=True)

        # エラー処理
        users = []
        for result in results:
            if isinstance(result, Exception):
                logger.warning("User fetch failed", error=str(result))
            else:
                users.append(result)
        return users
        # AI実装 → gather + error handling理解

    async def get_user_complete_data(self, user_id: int) -> dict:
        """ユーザー完全データ並行取得"""
        user, posts, todos = await asyncio.gather(
            self.get_user(user_id),
            self.get_posts(user_id),
            self.get_todos(user_id)
        )
        return {
            "user": user,
            "posts": posts,
            "todos": todos
        }
        # AI実装 → concurrent data fetching理解
```

```python
# パフォーマンス測定 (AI 80%)
import time

async def test_concurrent_performance():
    """並行処理パフォーマンステスト"""
    user_ids = list(range(1, 11))  # 10ユーザー

    # 非同期並行処理
    start = time.perf_counter()
    async with AsyncJSONPlaceholderClient() as client:
        users = await client.get_multiple_users(user_ids)
    async_duration = time.perf_counter() - start

    # 目標: 10リクエストが2秒以内
    assert async_duration < 2.0
    assert len(users) == 10
    # AI生成 → performance test理解

async def test_concurrent_error_handling():
    """並行処理エラーハンドリング"""
    user_ids = [1, 999999, 2]  # 1つエラー含む

    async with AsyncJSONPlaceholderClient() as client:
        users = await client.get_multiple_users(user_ids)

    # エラーをスキップして成功したもののみ返す
    assert len(users) == 2
    # AI生成 → error recovery pattern理解

# 6並行テスト作成:
# - test_gather_multiple_users
# - test_gather_complete_data
# - test_concurrent_performance
# - test_concurrent_error_handling
# - test_concurrent_timeout
# - test_gather_empty_list
```

**ポートフォリオ成果物**:
- [ ] get_multiple_users実装
- [ ] get_user_complete_data実装
- [ ] asyncio.gather()活用パターン習得
- [ ] 並行処理テスト6件作成
- [ ] パフォーマンス測定実装
- [ ] 同期vs非同期比較データ取得
- [ ] 累計43テスト達成
- [ ] カバレッジ53.50%達成

---

#### **Day 12 (土曜): Production Patterns + Week 2振り返り**

**総学習時間**: 7時間
**カバレッジ目標**: 54.74%（累積）

**Phase 1: AI説明・概念理解 (2.5h)**
- Production Async Patterns（接続プール管理、リソース制限、graceful shutdown）**[50分]**
- Async Logging Best Practices（非同期ログ出力、構造化ログ、パフォーマンス影響）**[50分]**
- Week 2振り返り方法論（習熟度評価、弱点分析、Week 3準備）**[40分]**
- 理解度測定（15分）**[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] async/awaitの動作原理(イベントループ+コルーチン+タスク)を完全に説明できる
  - [ ] pytest-asyncio全パターン(マーカー/フィクスチャ/モック)の使い分けを理解している
  - [ ] 非同期エラーハンドリング戦略(CancelledError/TimeoutError/HTTPError)を設計できる
  - [ ] パフォーマンス最適化実装(並行度/Connection Pool/Rate Limit)の根拠を説明できる
  - [ ] AsyncAPIClient完成版(全エンドポイント+並行処理+エラー処理)の実装品質を評価できる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- Production Pattern実装（connection pooling、max_connections設定、timeout統一）**[70分]**
- 最終調整テスト作成（残り7テスト → 累計50テスト達成）**[60分]**
- カバレッジ54.74%達成確認 **[30分]**
- バッファ **[20分]**

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- Week 2総復習・弱点補強

---

**実装タスク**:
```python
# Production Async Patterns (AI 75%)
import httpx
from typing import Optional

class AsyncAPIClient:
    def __init__(
        self,
        base_url: str,
        timeout: float = 30.0,
        max_connections: int = 10,
        max_keepalive_connections: int = 5
    ):
        self.base_url = base_url
        self._client: Optional[httpx.AsyncClient] = None
        self._timeout = timeout
        self._limits = httpx.Limits(
            max_connections=max_connections,
            max_keepalive_connections=max_keepalive_connections
        )
        # AI実装 → connection pooling理解

    async def __aenter__(self):
        self._client = httpx.AsyncClient(
            base_url=self.base_url,
            timeout=self._timeout,
            limits=self._limits
        )
        return self
        # AI実装 → production-ready client理解

    async def __aexit__(self, exc_type, exc_val, exc_tb):
        if self._client:
            await self._client.aclose()
        # graceful shutdown保証
```

```python
# Async Logging統合 (AI 75%)
import structlog

logger = structlog.get_logger()

class AsyncAPIClient:
    async def get(self, endpoint: str) -> dict:
        logger.info(
            "async_request_start",
            endpoint=endpoint,
            method="GET"
        )

        try:
            response = await self._client.get(endpoint)
            response.raise_for_status()

            logger.info(
                "async_request_success",
                endpoint=endpoint,
                status_code=response.status_code,
                duration=response.elapsed.total_seconds()
            )
            return response.json()
        except Exception as e:
            logger.error(
                "async_request_error",
                endpoint=endpoint,
                error=str(e)
            )
            raise
        # AI実装 → structured async logging理解
```

```python
# 最終調整テスト (AI 80%)
# 7テスト追加でWeek 2完成:
# - test_production_connection_pooling
# - test_production_max_connections
# - test_production_timeout_unified
# - test_production_graceful_shutdown
# - test_async_logging_integration
# - test_week2_comprehensive_integration
# - test_week2_performance_regression

# 累計50テスト達成（Week 2目標: 30テスト → 実際: 50テスト）
```

```
Week 2振り返り
   - [ ] 50テスト達成確認
   - [ ] カバレッジ54.74%以上確認
   - [ ] 躓き箇所リストアップ
```

**Week 2学習完了確認**:
- [ ] Async理解度80%以上

**ポートフォリオ成果物**:
- [ ] Production async client実装
- [ ] Connection pooling設定
- [ ] Async logging統合
- [ ] 最終調整テスト7件作成
- [ ] 全品質ゲート合格（pytest/ruff/mypy）

---

### Week 3: Async/Await深化 + 並行処理 (42時間)

**学習目標**:
- Async/Await深化・並行処理習得
- asyncio.gather()パフォーマンス理解
- 40テスト達成 (Week 1-3累計、カバレッジ68%+)

**認知負荷**: 73%
**自律目標**: Async理解40%、並行処理理解35%

**学習フロー**: @[CLAUDE.md#AI協働学習フロー仕組み]
- Phase 1: AI説明・概念理解（目標理解度30%）
- Phase 2: AI協働実装（目標理解度60%）
- Phase 3: 理解度確認・記録（目標理解度70%+）

---

#### **Day 13 (月曜): Async/Await基礎 + asyncio.gather()基礎**

**総学習時間**: 7時間
**カバレッジ目標**: 56.58%（累積）

**Phase 1: AI説明・概念理解（2.5h）**:
```yaml
学習項目:
  - asyncio基礎・イベントループ概念（50分）
  - async/await構文・パターン理解（50分）
  - asyncio.gather()基礎・並行処理概念（40分）
  - 理解度測定（5項目チェックリスト）（15分）
  - バッファ（15分）

AI依頼例:
  「asyncioのイベントループについて、以下の観点で説明してください:
  1. イベントループとは何か（Why）
  2. いつ使うのか（When）
  3. 基本的な使い方（How）
  4. よくある落とし穴（Pitfall）
  5. ベストプラクティス（Best Practice）

  説明後、簡単なコード例を3つ提示してください。」

理解度チェックリスト:
  - [ ] イベントループの役割を説明できる
  - [ ] async/await構文の使い分けを理解している
  - [ ] asyncio.gather()の基本動作を理解している
  - [ ] 同期処理との違いを2つ以上説明できる
  - [ ] 非同期処理の適用場面を判断できる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）
```

**Phase 2: AI協働実装 (3.0h)**
- AsyncAPIClient基礎実装（async/await基本パターン習得） **[70分]**
- 非同期GET/POST実装（httpx.AsyncClient活用） **[60分]**
- pytest-asyncio導入とテスト作成（非同期テスト3件） **[30分]**
- バッファ **[20分]**

**実装タスク**:
```python
# AsyncAPIClient基礎実装
class AsyncAPIClient:
    """非同期APIクライアント基礎実装"""

    def __init__(self, base_url: str, timeout: int = 30):
        self.base_url = base_url
        self.timeout = timeout

    async def get(self, endpoint: str) -> dict:
        """非同期GET実装"""
        async with httpx.AsyncClient(timeout=self.timeout) as client:
            response = await client.get(f"{self.base_url}{endpoint}")
            response.raise_for_status()
            return response.json()

    async def post(self, endpoint: str, data: dict) -> dict:
        """非同期POST実装"""
        async with httpx.AsyncClient(timeout=self.timeout) as client:
            response = await client.post(
                f"{self.base_url}{endpoint}",
                json=data
            )
            response.raise_for_status()
            return response.json()

# pytest-asyncio基礎テスト
@pytest.mark.asyncio
async def test_async_get_user():
    """非同期GET成功テスト"""
    client = AsyncAPIClient("https://jsonplaceholder.typicode.com")
    result = await client.get("/users/1")
    assert result["id"] == 1
    assert "name" in result

@pytest.mark.asyncio
async def test_async_post_user():
    """非同期POST成功テスト"""
    client = AsyncAPIClient("https://jsonplaceholder.typicode.com")
    data = {"title": "test", "body": "content", "userId": 1}
    result = await client.post("/posts", data)
    assert "id" in result
```

```bash
# 品質ゲート実行
uv run pytest --cov=. --cov-fail-under=56
uv run ruff check .
uv run mypy utils/ config/
```

**Phase 3: 理解度確認・記録（0.5h）**:
```markdown
Trigger 6: 理解度確認（将来実装）
- AI自動生成問題（3問・25点満点）
- 合格基準: 20点以上/25点（80%以上）
- 不合格時: 復習ループ（最大3回）

現在: 手動記録
- 学習時間記録
- 習熟度自己評価
- 課題・ブロッカー記録
```

**復習バッファ (1.0h)**:
- 品質ゲート失敗時の修正時間
- 理解不足項目の復習時間

**ポートフォリオ成果物**:
- [ ] asyncio基礎サンプル3個実行・理解
- [ ] AsyncAPIClientクラス実装
- [ ] async get/post実装完了
- [ ] pytest-asyncio導入完了
- [ ] 非同期テスト3件作成
- [ ] 品質ゲート合格（カバレッジ56%+）

---

#### **Day 14 (火曜): 並行処理実装 + エラーハンドリング**

**総学習時間**: 7時間
**カバレッジ目標**: 59.49%（累積）

**Phase 1: AI説明・概念理解（2.5h）**:
```yaml
学習項目:
  - asyncio.gather()詳細・例外処理（50分）
  - 並行処理パターン・ベストプラクティス（50分）
  - 非同期エラーハンドリング戦略（40分）
  - 理解度測定（5項目チェックリスト）（15分）
  - バッファ（15分）

理解度チェックリスト:
  - [ ] asyncio.gather()の例外処理を説明できる
  - [ ] 並行処理の適切な粒度を判断できる
  - [ ] 非同期リトライロジックを設計できる
  - [ ] 同期版との性能比較手法を理解している
  - [ ] 並行処理のデバッグ方法を知っている

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）
```

**Phase 2: AI協働実装 (3.0h)**
- 並行処理メソッド実装（asyncio.gather活用） **[70分]**
- パフォーマンス測定・比較実装 **[60分]**
- 非同期リトライロジック＋テスト作成 **[30分]**
- バッファ **[20分]**

**実装タスク**:
```python
# 並行処理メソッド実装
async def get_user_data(self, user_id: int) -> dict:
    """ユーザー情報+投稿+TODOを並行取得"""
    user, posts, todos = await asyncio.gather(
        self.get_user(user_id),
        self.get_posts(user_id),
        self.get_todos(user_id),
        return_exceptions=False  # エラーは即座に伝播
    )
    return {
        "user": user,
        "posts": posts,
        "todos": todos
    }

# パフォーマンス比較実装
import time

async def measure_performance():
    """同期版vs非同期版パフォーマンス測定"""
    # 同期版: 3リクエスト × 1秒 = 3秒
    # 非同期版: 3リクエスト並行 = 1秒
    start = time.time()
    async with AsyncJSONPlaceholderClient() as client:
        await client.get_user_data(1)
    duration = time.time() - start
    print(f"Duration: {duration:.2f}s")

# 非同期リトライロジック
async def get_with_retry(
    self,
    endpoint: str,
    retries: int = 3,
    delay: float = 1.0
) -> dict:
    """非同期リトライロジック実装"""
    for attempt in range(retries):
        try:
            return await self.get(endpoint)
        except httpx.TimeoutException:
            if attempt == retries - 1:
                raise APITimeoutError(
                    f"Timeout after {retries} retries"
                )
            await asyncio.sleep(delay * (attempt + 1))

# テスト実装
@pytest.mark.asyncio
async def test_get_user_data_parallel():
    """並行処理テスト"""
    client = AsyncAPIClient("https://jsonplaceholder.typicode.com")
    result = await client.get_user_data(1)
    assert "user" in result
    assert "posts" in result
    assert "todos" in result

@pytest.mark.asyncio
async def test_retry_logic():
    """リトライロジックテスト"""
    # タイムアウトシミュレーション
    pass
```

```bash
# 品質ゲート実行
uv run pytest --cov=. --cov-fail-under=59
uv run ruff check .
uv run mypy utils/ config/
```

**Phase 3: 理解度確認・記録（0.5h）**:
```markdown
Trigger 6: 理解度確認（将来実装）

現在: 手動記録
- 学習時間記録
- 習熟度自己評価
- 課題・ブロッカー記録
```

**復習バッファ (1.0h)**:
- 品質ゲート失敗時の修正時間
- 並行処理デバッグ時間

**ポートフォリオ成果物**:
- [ ] get_user_data並行処理実装
- [ ] パフォーマンス測定実装・比較
- [ ] 非同期リトライロジック実装
- [ ] エラーハンドリングテスト3件作成
- [ ] 品質ゲート合格（カバレッジ59%+）

---

#### **Day 15 (水曜): Async Context Manager + structlog基礎**

**総学習時間**: 7時間
**カバレッジ目標**: 61.58%（累積）

**Phase 1: AI説明・概念理解（2.5h）**:
```yaml
学習項目:
  - Async Context Manager概念・__aenter__/__aexit__（50分）
  - structlog基礎・構造化ログ概念（50分）
  - 非同期リソース管理パターン（40分）
  - 理解度測定（5項目チェックリスト）（15分）
  - バッファ（15分）

理解度チェックリスト:
  - [ ] __aenter__/__aexit__の役割を説明できる
  - [ ] Async Context Managerの使い分けを理解している
  - [ ] structlogの利点を3つ以上説明できる
  - [ ] 構造化ログの設計方針を理解している
  - [ ] 非同期リソース管理のベストプラクティスを知っている

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）
```

**Phase 2: AI協働実装 (3.0h)**
- Async Context Manager実装（__aenter__/__aexit__） **[70分]**
- structlog基礎設定とAPI統合 **[60分]**
- Context Manager＋structlogテスト作成 **[30分]**
- バッファ **[20分]**

**実装タスク**:
```python
# Async Context Manager実装
class AsyncJSONPlaceholderClient(AsyncAPIClient):
    """非同期コンテキストマネージャー実装"""

    async def __aenter__(self):
        """リソース初期化"""
        self.client = httpx.AsyncClient(
            base_url=self.base_url,
            timeout=self.timeout
        )
        return self

    async def __aexit__(self, exc_type, exc_val, exc_tb):
        """リソース解放"""
        if self.client:
            await self.client.aclose()
        return False  # 例外は再送出

# 使用例
async with AsyncJSONPlaceholderClient() as client:
    result = await client.get_user(1)

# structlog基礎設定
import structlog

# config/logging_config.py
def setup_logging():
    """structlog設定"""
    structlog.configure(
        processors=[
            structlog.processors.TimeStamper(fmt="iso"),
            structlog.stdlib.add_log_level,
            structlog.processors.JSONRenderer()
        ],
        logger_factory=structlog.PrintLoggerFactory(),
    )

# AsyncAPIClientへのstructlog統合
logger = structlog.get_logger()

async def get(self, endpoint: str) -> dict:
    """structlog統合GET実装"""
    logger.info("api_request_start", endpoint=endpoint)
    try:
        response = await self.client.get(endpoint)
        logger.info("api_request_success", endpoint=endpoint, status=response.status_code)
        return response.json()
    except Exception as e:
        logger.error("api_request_failed", endpoint=endpoint, error=str(e))
        raise

# テスト実装
@pytest.mark.asyncio
async def test_context_manager():
    """Context Managerテスト"""
    async with AsyncJSONPlaceholderClient() as client:
        result = await client.get_user(1)
        assert result["id"] == 1
    # クライアントが自動的にcloseされることを確認

@pytest.mark.asyncio
async def test_structlog_integration():
    """structlog統合テスト"""
    setup_logging()
    async with AsyncJSONPlaceholderClient() as client:
        await client.get_user(1)
    # ログ出力を検証
```

```bash
# 品質ゲート実行
uv run pytest --cov=. --cov-fail-under=61
uv run ruff check .
uv run mypy utils/ config/
```

**Phase 3: 理解度確認・記録（0.5h）**:
```markdown
Trigger 6: 理解度確認（将来実装）

現在: 手動記録
- 学習時間記録
- 習熟度自己評価
- 課題・ブロッカー記録
```

**復習バッファ (1.0h)**:
- 品質ゲート失敗時の修正時間
- Context Manager動作デバッグ時間

**中間評価チェックリスト**:
- [ ] Week 3進捗50%達成
- [ ] Async/Await理解度40%以上

**ポートフォリオ成果物**:
- [ ] AsyncJSONPlaceholderClient Context Manager実装
- [ ] structlog導入・基礎設定完了
- [ ] 主要箇所structlog統合
- [ ] Context Managerテスト2件作成
- [ ] 品質ゲート合格（カバレッジ61%+）

---

#### **Day 16 (木曜): AsyncJSONPlaceholderClient完成 + mypy導入**

**総学習時間**: 7時間
**カバレッジ目標**: 64.09%（累積）

**Phase 1: AI説明・概念理解（2.5h）**:
```yaml
学習項目:
  - AsyncJSONPlaceholderClient全メソッド設計（50分）
  - mypy基礎・型ヒント戦略（50分）
  - 非同期パフォーマンステスト設計（40分）
  - 理解度測定（5項目チェックリスト）（15分）
  - バッファ（15分）

理解度チェックリスト:
  - [ ] 非同期クライアントの全体設計を説明できる
  - [ ] mypy導入のメリット・デメリットを理解している
  - [ ] 型ヒントの適用範囲を判断できる
  - [ ] パフォーマンステストの計測手法を理解している
  - [ ] 並行処理のボトルネック分析手法を知っている

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）
```

**Phase 2: AI協働実装 (3.0h)**
- AsyncJSONPlaceholderClient完成（全メソッド実装） **[70分]**
- 並行処理＋エラーハンドリング統合 **[60分]**
- 統合テスト作成（非同期クライアント全機能） **[30分]**
- バッファ **[20分]**

**実装タスク**:
```python
# AsyncJSONPlaceholderClient完成
class AsyncJSONPlaceholderClient(AsyncAPIClient):
    """JSONPlaceholder API非同期クライアント完成版"""

    async def get_user(self, user_id: int) -> dict:
        """ユーザー情報取得"""
        return await self.get(f"/users/{user_id}")

    async def get_posts(self, user_id: int | None = None) -> list[dict]:
        """投稿一覧取得"""
        endpoint = f"/posts?userId={user_id}" if user_id else "/posts"
        return await self.get(endpoint)

    async def get_todos(self, user_id: int) -> list[dict]:
        """TODO一覧取得"""
        return await self.get(f"/users/{user_id}/todos")

    async def get_user_data(self, user_id: int) -> dict:
        """ユーザー情報+投稿+TODO並行取得"""
        user, posts, todos = await asyncio.gather(
            self.get_user(user_id),
            self.get_posts(user_id),
            self.get_todos(user_id)
        )
        return {"user": user, "posts": posts, "todos": todos}

# パフォーマンステスト実装
@pytest.mark.performance
async def test_parallel_requests_performance():
    """並行リクエストパフォーマンステスト"""
    start = time.time()
    async with AsyncJSONPlaceholderClient() as client:
        results = await asyncio.gather(
            *[client.get_user(i) for i in range(1, 11)]
        )
    duration = time.time() - start
    assert duration < 2.0  # 10リクエストが2秒以内
    assert len(results) == 10

# mypy導入・型ヒント追加
# pyproject.toml設定
[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

# 型ヒント例
async def get(self, endpoint: str) -> dict:
    """型ヒント付きGETメソッド"""
    response: httpx.Response = await self.client.get(endpoint)
    return response.json()
```

```bash
# mypy導入・実行
uv add --dev mypy
uv run mypy utils/ config/

# 品質ゲート実行
uv run pytest --cov=. --cov-fail-under=64
uv run ruff check .
uv run mypy utils/ config/
```

**Phase 3: 理解度確認・記録（0.5h）**:
```markdown
Trigger 6: 理解度確認（将来実装）

現在: 手動記録
- 学習時間記録
- 習熟度自己評価
- 課題・ブロッカー記録
```

**復習バッファ (1.0h)**:
- 品質ゲート失敗時の修正時間
- mypy型エラー修正時間

**ポートフォリオ成果物**:
- [ ] AsyncJSONPlaceholderClient全メソッド実装完了
- [ ] get_user_data並行処理実装
- [ ] mypy導入・pyproject.toml設定完了
- [ ] 全コード型ヒント追加完了
- [ ] パフォーマンステスト3件作成
- [ ] 品質ゲート合格（カバレッジ64%+、mypy合格）

---

#### **Day 17 (金曜): Week 3仕上げ + ドキュメント整備**

**総学習時間**: 7時間
**カバレッジ目標**: 66.49%（累積）

**Phase 1: AI説明・概念理解（2.5h）**:
```yaml
学習項目:
  - README技術ドキュメント設計（50分）
  - docstring/コメント戦略（50分）
  - 追加テストケース設計（40分）
  - 理解度測定（5項目チェックリスト）（15分）
  - バッファ（15分）

理解度チェックリスト:
  - [ ] READMEに含めるべき要素を列挙できる
  - [ ] docstringの適切な粒度を判断できる
  - [ ] テストカバレッジ戦略を説明できる
  - [ ] コードコメントの必要性を判断できる
  - [ ] ドキュメント品質評価基準を知っている

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）
```

**Phase 2: AI協働実装 (3.0h)**
- README更新（非同期処理機能・パフォーマンス比較） **[70分]**
- docstring＋コードコメント追加 **[60分]**
- ドキュメント品質チェック **[30分]**
- バッファ **[20分]**

```markdown
# README更新内容
## 非同期処理機能
- AsyncAPIClient実装詳細
- async/await使用パターン
- asyncio.gather()による並行処理

## パフォーマンス比較
- 同期版: 3リクエスト × 1秒 = 3秒
- 非同期版: 3リクエスト並行 = 1秒
- 約66%の性能向上

## 使用例
```python
# 非同期クライアント使用例
async with AsyncJSONPlaceholderClient() as client:
    result = await client.get_user_data(1)
    print(result)
```

# docstring追加
- 全public関数にdocstring追加
- 引数・戻り値・例外の記載
- Google Style docstring採用

# 追加テスト実装
- エッジケーステスト2件
- 統合テスト2件
- パフォーマンステスト1件
```

```bash
# 品質ゲート実行
uv run pytest --cov=. --cov-fail-under=66
uv run ruff check .
uv run mypy utils/ config/
```

**Phase 3: 理解度確認・記録（0.5h）**:
```markdown
Trigger 6: 理解度確認（将来実装）

現在: 手動記録
- 学習時間記録
- 習熟度自己評価
- 課題・ブロッカー記録
```

**復習バッファ (1.0h)**:
- 品質ゲート失敗時の修正時間
- ドキュメント品質レビュー時間

**ポートフォリオ成果物**:
- [ ] README非同期処理セクション追加
- [ ] 全関数docstring追加完了
- [ ] コードコメント整備完了
- [ ] 追加テスト5件作成
- [ ] 品質ゲート合格（カバレッジ66%+）

**🎯 Week 3進捗確認**:
- [ ] Async/Await理解度40%達成
- [ ] 並行処理理解度35%達成
- [ ] 40テスト達成（Week 1-3累積）
- [ ] カバレッジ66%+達成

---

#### **Day 18 (土曜): Week 3振り返り + pytest fixture入門**

**総学習時間**: 7時間
**カバレッジ目標**: 68.00%（累積、Week 3最終目標）

**Phase 1: AI説明・概念理解（2.5h）**:
```yaml
学習項目:
  - pytest fixture基礎概念（50分）
  - fixture scope戦略（50分）
  - Week 3振り返り・分析（40分）
  - 理解度測定（5項目チェックリスト）（15分）
  - バッファ（15分）

理解度チェックリスト:
  - [ ] pytest fixtureの目的を説明できる
  - [ ] fixture scopeの使い分けを理解している
  - [ ] Week 3での学習成果を列挙できる
  - [ ] Week 3での課題を特定できている
  - [ ] Week 4の学習計画を立てられる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）
```

**Phase 2: AI協働実装 (3.0h)**
- pytest fixture基礎実装（function/module/session scope） **[70分]**
- fixture活用テスト作成 **[60分]**
- Week 3総合実装・統合テスト **[30分]**
- バッファ **[20分]**

**実装タスク**:
```python
# pytest fixture基礎実装
@pytest.fixture
def sample_user() -> dict:
    """サンプルユーザーフィクスチャ"""
    return {"id": 1, "name": "Test User", "email": "test@example.com"}

@pytest.fixture(scope="module")
def async_client() -> AsyncJSONPlaceholderClient:
    """非同期クライアントフィクスチャ（module scope）"""
    client = AsyncJSONPlaceholderClient()
    yield client
    # teardown処理

# fixture使用テスト
def test_with_fixture(sample_user):
    """fixtureを使用したテスト"""
    assert sample_user["id"] == 1
    assert "name" in sample_user

# GitHub整備
# - README.md更新・レビュー
# - リポジトリ説明追加
# - トピック設定
```

```bash
# 品質ゲート実行（Week 3最終確認）
uv run pytest --cov=. --cov-fail-under=68
uv run ruff check .
uv run mypy utils/ config/
```

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- Week 3総復習・弱点補強

---

```
Week 3振り返り
   - [ ] 40テスト達成確認（Week 1-3累積）
   - [ ] カバレッジ68%以上確認
   - [ ] 躓き箇所リストアップ
```

**Week 3学習完了確認**:
- [ ] Async/Await理解度80%以上
- [ ] 並行処理理解度80%以上
- [ ] pytest fixture基礎理解80%以上

**ポートフォリオ成果物**:
- [ ] AsyncAPIClient完成
- [ ] AsyncJSONPlaceholderClient完成
- [ ] structlog統合完了
- [ ] mypy導入完了
- [ ] GitHub整備完了
- [ ] 全品質ゲート合格（pytest/ruff/mypy）

---

### Week 4: Error Handling深化 (42時間)

**学習目標**:
- エラーハンドリング階層設計の深化
- リトライロジックの最適化
- 60テスト達成 (カバレッジ75%+)

**認知負荷**: 65%
**自律目標**: Error Handling理解45%、実装自律30%

**学習フロー**: @[CLAUDE.md#AI協働学習フロー仕組み]
- Phase 1: AI説明・概念理解（目標理解度30%）
- Phase 2: AI協働実装（目標理解度60%）
- Phase 3: 理解度確認・記録（目標理解度70%+）

---

#### **Day 19 (月曜): エラー階層拡張 + 詳細エラー情報設計**

**総学習時間**: 7時間
**カバレッジ目標**: 69%（累積、+1%）

**Phase 1: AI説明・概念理解 (2.5h)**
- エラー階層拡張概念（APIValidationError、APIRateLimitError設計理由）**[50分]**
- 詳細エラー情報保持パターン（response_data活用戦略、デバッグ効率化）**[50分]**
- エラーメッセージ設計ベストプラクティス（ユーザー向け/開発者向け分離）**[40分]**
- 理解度測定（15分）**[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] APIValidationError/APIRateLimitErrorを追加する設計理由（エラー分類粒度向上）を説明できる
  - [ ] response_data保持による詳細エラー情報活用戦略（デバッグ効率化）を理解している
  - [ ] エラーメッセージ設計の2層構造（ユーザー向けmessage/開発者向けdetails）を設計できる
  - [ ] HTTPステータスコード別エラークラス設計（400番台/500番台分離）の保守性メリットを説明できる
  - [ ] エラー階層継承構造（基底APIClientError → 特化型例外）の拡張性を理解している

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- エラー階層拡張実装（APIValidationError、APIRateLimitError追加）**[70分]**
- 詳細エラー情報実装（APIHTTPError.__init__拡張、response_data保持）**[60分]**
- エラーテスト10件作成（新規例外クラステスト、詳細情報保持テスト）**[30分]**
- バッファ **[20分]**

**実装タスク**:

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- 品質ゲート失敗時の修正時間
- 理解度不足項目の復習時間

**ポートフォリオ成果物**:
- [ ] APIValidationError/APIRateLimitError実装
- [ ] 詳細エラー情報保持機能実装
- [ ] エラーテスト10件追加
- [ ] 累計60テスト達成
- [ ] カバレッジ69%達成

---

#### **Day 20 (火曜): Exponential Backoff + Jitter実装**

**総学習時間**: 7時間
**カバレッジ目標**: 71%（累積、+2%）

**Phase 1: AI説明・概念理解 (2.5h)**
- Exponential backoff理論（指数バックオフ戦略、リトライ間隔計算）**[50分]**
- Jitter概念（リトライ分散、サーバー負荷軽減戦略）**[50分]**
- リトライロジック最適化戦略（max_delay制御、base_delay調整）**[40分]**
- 理解度測定（15分）**[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] Exponential backoffの指数バックオフ戦略（2の累乗でリトライ間隔増加）を説明できる
  - [ ] Jitterによるリトライ分散効果（サーバー負荷軽減、thundering herd回避）を理解している
  - [ ] max_delay制御の必要性（無限大のバックオフ防止）を説明できる
  - [ ] base_delay調整によるリトライ頻度チューニング戦略を理解している
  - [ ] リトライロジック最適化の保守性・拡張性向上効果を評価できる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- _calculate_backoff実装（Exponential backoff計算、max_delay制御）**[70分]**
- Jitter実装（ランダム分散、0.5-1.0倍率）**[60分]**
- リトライロジックテスト作成（backoff計算テスト、Jitter範囲テスト）**[30分]**
- バッファ **[20分]**

**実装タスク**:

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- 品質ゲート失敗時の修正時間
- 理解度不足項目の復習時間

**ポートフォリオ成果物**:
- [ ] _calculate_backoff実装
- [ ] Jitter実装
- [ ] リトライロジック既存テスト改善
- [ ] 累計60テスト維持
- [ ] カバレッジ71%達成

---

#### **Day 21 (水曜): 中間確認 + テストカバレッジ戦略**

**総学習時間**: 7時間
**カバレッジ目標**: 73%（累積、+2%）

**Phase 1: AI説明・概念理解 (2.5h)**
- テストカバレッジ戦略（境界値テスト、エッジケーステスト設計）**[50分]**
- 中間確認方法論（Week 4前半レビュー、品質ゲート確認）**[50分]**
- Week 4前半振り返り（Day 19-20達成事項、課題分析）**[40分]**
- 理解度測定（15分）**[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] 境界値テスト設計戦略（正常値/異常値境界、エッジケース網羅）を説明できる
  - [ ] テストカバレッジ向上戦略（未テスト分岐抽出、テスト優先度付け）を理解している
  - [ ] Week 4前半達成事項（エラー階層拡張、Exponential Backoff実装）を評価できる
  - [ ] 中間確認での品質ゲート実行結果（pytest/ruff/mypy合格確認）を分析できる
  - [ ] Week 4後半計画調整（Day 22-24学習項目、カバレッジ目標）を立案できる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- カバレッジ分析（未テスト分岐抽出、テスト優先度付け）**[70分]**
- 境界値テスト作成（正常値/異常値境界テスト、エッジケーステスト）**[60分]**
- カバレッジ73%以上達成（追加テスト実装、品質ゲート実行）**[30分]**
- バッファ **[20分]**

**実装タスク**:

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- Week 4前半弱点復習時間
- Day 22準備時間

**ポートフォリオ成果物**:
- [ ] 境界値テスト作成
- [ ] エッジケーステスト作成
- [ ] 累計60テスト維持
- [ ] カバレッジ73%達成
- [ ] Week 4前半振り返りレポート作成

---

#### **Day 22 (木曜): エラーハンドリング統合 + structlog強化**

**総学習時間**: 7時間
**カバレッジ目標**: 74%（累積、+1%）

**Phase 1: AI説明・概念理解 (2.5h)**
- エラーハンドリング統合パターン（BaseAPIClient/AsyncAPIClient統一）**[50分]**
- エラーログ強化戦略（structlog統合、構造化ログ設計）**[50分]**
- 統合テスト設計（全クライアント統合テスト、エラーログ検証）**[40分]**
- 理解度測定（15分）**[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] BaseAPIClient/AsyncAPIClientのエラーハンドリング統一戦略（共通基底クラス設計）を説明できる
  - [ ] structlog統合によるエラーログ強化効果（構造化ログ、デバッグ効率化）を理解している
  - [ ] エラーログ設計の3層構造（エラー発生箇所/エラー詳細/スタックトレース）を設計できる
  - [ ] 統合テスト設計の重要性（全クライアント横断テスト、エラー伝播確認）を説明できる
  - [ ] エラーハンドリング統合の保守性・拡張性向上効果を評価できる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- BaseAPIClient/AsyncAPIClientエラーハンドリング統一実装**[70分]**
- structlog統合実装（構造化ログ設計、エラーログ強化）**[60分]**
- 統合テスト5件作成（全クライアント横断テスト、エラーログ検証テスト）**[30分]**
- バッファ **[20分]**

**実装タスク**:

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- 品質ゲート失敗時の修正時間
- 理解度不足項目の復習時間

**ポートフォリオ成果物**:
- [ ] BaseAPIClient/AsyncAPIClientエラーハンドリング統一
- [ ] structlog統合実装
- [ ] 統合テスト5件作成
- [ ] 累計60テスト維持
- [ ] カバレッジ74%達成

---

#### **Day 23 (金曜): Week 4仕上げ + Week 4振り返り**

**総学習時間**: 7時間
**カバレッジ目標**: 75%（累積、+1%、Week 4最終目標）

**Phase 1: AI説明・概念理解 (2.5h)**
- Week 4総復習（エラー階層拡張、Exponential Backoff、Jitter、統合パターン）**[50分]**
- コード品質最終確認概念（ruff/mypy全合格確認、リファクタリング戦略）**[50分]**
- Week 4振り返り方法論（習熟度評価、弱点分析、Week 5準備）**[40分]**
- 理解度測定（15分）**[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] Week 4全体（エラー階層拡張、Exponential Backoff、Jitter、統合）の技術的成果を説明できる
  - [ ] エラーハンドリング階層設計の保守性・拡張性向上効果を理解している
  - [ ] Exponential Backoff + Jitterによるリトライロジック最適化効果を説明できる
  - [ ] structlog統合によるデバッグ効率化の具体的効果を評価できる
  - [ ] Week 4全体の達成事項（60テスト達成、カバレッジ75%達成）と課題（弱点分析）を整理でき、Week 5への学習計画を立てられる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- コード品質最終確認（ruff/mypy全合格確認、リファクタリング実施）**[70分]**
- カバレッジ75%達成確認（追加テスト実装、品質ゲート実行）**[60分]**
- Week 4振り返りレポート作成（学習記録、弱点分析、Week 5計画）**[30分]**
- バッファ **[20分]**

**実装タスク**:

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- Week 4全体弱点復習時間
- Week 5準備時間

**ポートフォリオ成果物**:
- [ ] ruff/mypy全合格確認
- [ ] カバレッジ75%達成
- [ ] Week 4振り返りレポート作成
- [ ] 累計60テスト維持
- [ ] Week 4全体品質確認完了

---

#### **Day 24 (土曜): Week 4振り返り + Week 5準備**

**総学習時間**: 7時間
**カバレッジ目標**: 77%（Week 4最終目標）

**Phase 1: AI説明・概念理解 (2.5h)**
- pytest fixture概念復習（scope動作、factory pattern、parametrize）**[50分]**
- Mock/Patch基礎概念（モックオブジェクト、パッチング戦略）**[60分]**
- Week 4詳細分析（習熟度評価、弱点分析、改善計画）**[25分]**
- 理解度測定（15分）**[15分]**

理解度チェックリスト:
  - [ ] pytest fixtureの3つのscope（function/module/session）動作を説明できる
  - [ ] factory pattern実装戦略（テストデータ生成、パラメータ化）を理解している
  - [ ] Mock/Patchの基本概念（モックオブジェクト作成、return_value設定）を説明できる
  - [ ] Week 4全体の達成事項と課題を評価でき、弱点分析結果を説明できる
  - [ ] Week 5学習計画（pytest fixture深掘り、Mock/Patch基本、Pydantic Settings導入）を立案できる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**
- pytest fixture予習実装（簡単なfixture作成、scope動作確認）**[70分]**
- Week 5学習計画作成（Day 25-30詳細計画、学習目標設定）**[60分]**
- Week 4最終確認（品質ゲート実行、カバレッジ確認、テスト数確認）**[30分]**
- バッファ **[20分]**

**実装タスク**:

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- Week 4総復習・弱点補強

---

```
Week 4振り返り
   - [ ] 60テスト達成確認
   - [ ] カバレッジ77%以上確認
   - [ ] 躓き箇所リストアップ
```

**Week 4学習完了確認**:
- [ ] Error Handling理解度80%以上
- [ ] リトライロジック理解度80%以上

**ポートフォリオ成果物**:
- [ ] pytest fixture予習実装
- [ ] 全品質ゲート合格（pytest/ruff/mypy）

---

### Week 5: pytest + Pydantic Settings (42時間)

**学習目標**:
- pytest fixture深掘り
- Mock/Patch基本
- Pydantic Settings導入・統合
- 80テスト達成 (カバレッジ78%+)

**認知負荷**: 65%

**学習フロー**: @[CLAUDE.md#AI協働学習フロー仕組み]
- Phase 1: AI説明・概念理解(目標理解度30%)
- Phase 2: AI協働実装(目標理解度60%)
- Phase 3: 理解度確認・記録(目標理解度70%+)

---

#### **Day 25 (月曜): pytest fixture深掘り**

**総学習時間**: 7時間

**Phase 1: AI説明・概念理解 (2.5h)**
- pytest fixture scope理解（function/module/session） **[50分]**
- fixture factory pattern概念 **[40分]**
- parametrize idsパターン理解 **[40分]**
- 理解度測定（15分） **[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] fixture scopeの違いと使い分けを説明できる
  - [ ] factory patternの利点を説明できる
  - [ ] parametrize idsの役割を理解している
  - [ ] fixtureのライフサイクルを理解している
  - [ ] yieldの動作を説明できる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**

**実装タスク**:
```python
# conftest.py作成
import pytest

@pytest.fixture(scope="function")
def api_client():
    """各テストで新規作成"""
    client = JSONPlaceholderClient()
    yield client
    client.close()
    # 自力実装 → scope動作理解

@pytest.fixture(scope="module")
def shared_client():
    """モジュール内で共有"""
    client = JSONPlaceholderClient()
    yield client
    client.close()
    # 自力実装 → 共有理解

@pytest.fixture(scope="session")
def test_config():
    """全テストで共有"""
    # AI実装 → session scope理解
```

```python
# Test data factory
@pytest.fixture
def user_factory():
    def _create_user(
        user_id: int = 1,
        name: str = "Test User",
        email: str = "test@example.com",
    ) -> dict:
        return {
            "id": user_id,
            "name": name,
            "email": email,
        }
    return _create_user
    # 自力実装 → factory理解

@pytest.fixture
def todo_factory():
    # 自力実装（user_factoryパターン踏襲）
    pass

@pytest.fixture
def post_factory():
    # AI支援（複雑な構造の場合）
    pass

def test_user_creation(user_factory):
    user1 = user_factory(user_id=1)
    user2 = user_factory(user_id=2, name="Another")
    # 自力実装 → テストパターン理解
```

```python
# Parametrize advanced
@pytest.mark.parametrize(
    "user_id,expected_email",
    [
        (1, "Sincere@april.biz"),
        (2, "Shanna@melissa.tv"),
    ],
    ids=["user1", "user2"],
)
def test_user_email(user_id, expected_email):
    # 自力実装 → ids理解
```

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度確認問題実施（25点満点、20点以上で合格）
- 学習記録更新（learning_state.yaml + daily_progress.md）

**復習バッファ (1h)**

**ポートフォリオ成果物**:
- [ ] conftest.py作成
- [ ] 3 scope fixture実装（function/module/session）
- [ ] scope動作確認テスト3件
- [ ] user_factory実装
- [ ] todo_factory実装
- [ ] post_factory実装
- [ ] factory使用テスト3件
- [ ] Parametrizedテスト4件（ids指定必須）
- [ ] 累計55テスト（+10テスト）

---

#### **Day 26 (火曜): Mock/Patch基本**

**総学習時間**: 7時間
**カバレッジ目標**:

**実装タスク**:
```python
# Mock基本
from unittest.mock import Mock, patch

def test_api_call_with_mock():
    mock_client = Mock()
    mock_client.get.return_value = {"id": 1, "name": "Test"}

    result = mock_client.get("/users/1")
    assert result["id"] == 1
    mock_client.get.assert_called_once()
    # 自力実装 → mockパターン理解
```

```python
# patch使用 (AI 70%)
@patch("httpx.Client.get")
def test_get_user_with_patch(mock_get):
    mock_response = Mock()
    mock_response.json.return_value = {"id": 1}
    mock_get.return_value = mock_response

    # AI実装 → patch理解
```

```python
# httpx mock fixture (AI 70%)
@pytest.fixture
def mock_httpx_client():
    with patch("httpx.Client") as mock:
        yield mock
    # AI実装 → fixture統合理解
```

**ポートフォリオ成果物**:
- [ ] Mock基礎テスト3件（return_value, side_effect, assert_called）
- [ ] patch基本テスト2件（デコレータパターン）
- [ ] mock_httpx_client fixture実装
- [ ] Mock vs Patch比較ドキュメント作成
- [ ] 累計60テスト（+5テスト）

---

#### **Day 27 (水曜): Pydantic Settings導入 + ConfigManager設計準備**

**総学習時間**: 7時間
**カバレッジ目標**: 76%

**Phase 1: AI説明・概念理解 (2.5h)**
- Pydantic Settings基礎理解 **[50分]**
- BaseSettings vs BaseModel違い **[40分]**
- ネスト設定パターン理解 **[40分]**
- 理解度測定（15分） **[15分]**
- バッファ **[15分]**

理解度チェックリスト:
  - [ ] BaseSettingsの役割を説明できる
  - [ ] env_file自動読み込みを理解している
  - [ ] ネスト設定の利点を説明できる
  - [ ] Field constraintsの使い方を理解している
  - [ ] バリデーションエラー処理を理解している

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3h)**

**実装タスク**:
```python
# config/settings.py作成
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    environment: str = "development"
    debug: bool = True

    # API設定
    api_base_url: str = "https://jsonplaceholder.typicode.com"
    api_timeout: int = 30
    api_retry_count: int = 3

    class Config:
        env_file = ".env"
        env_file_encoding = "utf-8"
    # 自力実装 → 設定構造理解

settings = Settings()
```

```python
# ネスト設定 (AI 70%)
class APIConfig(BaseSettings):
    base_url: str
    timeout: int = 30
    retry_count: int = 3
    retry_delay: float = 1.0

class LogConfig(BaseSettings):
    level: str = "INFO"
    format: str = "json"

class Settings(BaseSettings):
    environment: str = "development"
    api: APIConfig
    log: LogConfig

    # AI実装 → ネスト構造理解
```

**ConfigManager基礎設計 (30分) - Day 28準備**:
```python
# ConfigManager設計メモ作成
"""
ConfigManager設計方針:
- 役割: 設定の読み込み・検証・取得を一元管理
- 目的: 設定アクセスの型安全性とバリデーション強化
- 構成要素:
  1. Environment Enum (development/testing/staging/production)
  2. APIConfigSchema, LogConfigSchema (Field constraints付き)
  3. ConfigManager class (load_config, get_config, shortcuts)
  4. Cross-config validation (設定間依存関係検証)
"""
# AI協働 → Day 28実装の事前整理
```

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度確認問題実施（25点満点、20点以上で合格）
- 学習記録更新（learning_state.yaml + daily_progress.md）

**復習バッファ (1h)**

**ポートフォリオ成果物**:
- [ ] Settings class作成
- [ ] .env.example作成
- [ ] 環境変数読み込みテスト2件
- [ ] ネスト設定実装（APIConfig, LogConfig）
- [ ] 設定アクセステスト2件
- [ ] バリデーションテスト1件
- [ ] 累計65テスト（+5テスト）
- [ ] カバレッジ76%達成

---

#### **Day 28 (木曜): ConfigManager完全実装**

**総学習時間**: 7時間
**カバレッジ目標**: 85%

**Phase 1: AI説明・概念理解 (2h)** ※短縮版 2.5h→2h（Day 27設計準備により概念学習時間削減）
- Pydantic Settings復習（Day 27内容確認） **[30分]**
- ConfigManager設計パターン理解 **[50分]**
  - 役割: 設定の読み込み・検証・取得の一元管理
  - Environment Enum設計
  - Schema vs Manager責任分離
- 依存関係検証パターン理解 **[30分]**
  - Cross-config validation戦略
  - Production環境での制約（DEBUG禁止等）
- 理解度測定（15分） **[15分]**
- バッファ **[5分]**

理解度チェックリスト:
  - [ ] ConfigManagerの役割と責任範囲を説明できる
  - [ ] Environment Enumの用途を理解している
  - [ ] Schema（不変データ）とManager（操作）の違いを説明できる
  - [ ] Cross-config validation の必要性を理解している
  - [ ] frozen=Trueの意味と利点を説明できる

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3.5h)** ※拡大版 3h→3.5h（30分 from Phase 1 + バッファ活用）

**実装タスク（合計200行、25テスト）**:

```python
# 1. Environment Enum実装 (20分) - 自力実装
from enum import Enum

class Environment(str, Enum):
    """環境列挙型"""
    DEVELOPMENT = "development"
    TESTING = "testing"
    STAGING = "staging"
    PRODUCTION = "production"
    # 自力実装 → Enum理解
```

```python
# 2. APIConfig/LogConfig Schema実装 (50分) - AI 40%
from pydantic import BaseModel, Field
from typing import Optional

class APIConfigSchema(BaseModel):
    """API設定スキーマ（不変）"""
    base_url: str = Field(..., min_length=1)
    timeout: int = Field(gt=0, le=300)
    retry_count: int = Field(ge=0, le=10)
    retry_delay: float = Field(gt=0, le=60.0)
    max_connections: int = Field(gt=0, le=1000)

    class Config:
        frozen = True  # イミュータブル
    # AI 40%協働 → Field constraints理解

class LogConfigSchema(BaseModel):
    """ログ設定スキーマ（不変）"""
    level: str = Field(..., pattern="^(DEBUG|INFO|WARNING|ERROR|CRITICAL)$")
    format: str = Field(..., pattern="^(console|json)$")
    file: Optional[str] = None

    class Config:
        frozen = True
    # 自力実装（API patternパターン踏襲）
```

```python
# 3. ConfigManager基本実装 (60分) - AI 30%
import structlog
from typing import Dict, Any

logger = structlog.get_logger(__name__)

class ConfigValidationError(Exception):
    """設定検証エラー"""
    pass

class ConfigNotFoundError(Exception):
    """設定未検出エラー"""
    pass

class ConfigManager:
    """設定管理クラス"""

    def __init__(self, environment: Environment = Environment.DEVELOPMENT):
        self.environment = environment
        self._configs: Dict[str, Any] = {}
        self.logger = logger.bind(component="ConfigManager", env=environment.value)
        self.logger.info("config_manager_initialized")

    def load_config(self, config_name: str, config_data: Dict[str, Any]) -> None:
        """設定読み込み・検証"""
        try:
            schema_map = {
                "api": APIConfigSchema,
                "log": LogConfigSchema
            }

            if config_name not in schema_map:
                raise ConfigValidationError(f"Unknown config name: {config_name}")

            schema = schema_map[config_name]
            validated_config = schema(**config_data)

            self._configs[config_name] = validated_config
            self.logger.info("config_loaded", config_name=config_name)

        except ValidationError as e:
            self.logger.error("config_validation_failed", config_name=config_name, error=str(e))
            raise ConfigValidationError(f"Validation failed: {e}") from e

    def get_config(self, config_name: str) -> BaseModel:
        """設定取得"""
        if config_name not in self._configs:
            self.logger.warning("config_not_found", config_name=config_name)
            raise ConfigNotFoundError(f"Config '{config_name}' not loaded")

        return self._configs[config_name]
    # AI 30%協働 → 基本パターン理解
```

```python
# 4. 依存関係検証実装 (40分) - AI 50%
from typing import List

class ConfigManager:
    # ... 既存メソッド継続

    def get_api_config(self) -> APIConfigSchema:
        """API設定取得（型安全ショートカット）"""
        return self.get_config("api")

    def get_log_config(self) -> LogConfigSchema:
        """ログ設定取得（型安全ショートカット）"""
        return self.get_config("log")

    def validate_cross_config_dependencies(self) -> List[str]:
        """設定間依存関係検証"""
        warnings = []

        try:
            api_config = self.get_api_config()
            log_config = self.get_log_config()

            # Production環境でDEBUGログは非推奨
            if self.environment == Environment.PRODUCTION:
                if log_config.level == "DEBUG":
                    warnings.append("DEBUG log level in production is not recommended")

            # タイムアウト > 60秒の場合はログレベルINFO以上推奨
            if api_config.timeout > 60 and log_config.level == "DEBUG":
                warnings.append("High timeout with DEBUG log may cause performance issues")

        except ConfigNotFoundError:
            warnings.append("Cannot validate dependencies: configs not loaded")

        if warnings:
            self.logger.warning("cross_config_warnings", warnings=warnings)

        return warnings

    def list_loaded_configs(self) -> List[str]:
        """読み込み済み設定名リスト取得"""
        return list(self._configs.keys())
    # AI 50%協働 → 検証ロジック理解
```

```python
# 5. テスト作成 (40分) - AI 30%
# tests/unit/test_config_manager.py

# Basic CRUD Tests (5件)
def test_load_api_config():
    """API設定読み込み正常系"""
    # 自力実装

def test_load_log_config():
    """ログ設定読み込み正常系"""
    # 自力実装

def test_get_config_success():
    """設定取得成功"""
    # 自力実装

def test_get_config_not_found():
    """設定未読み込みエラー"""
    # 自力実装

def test_list_loaded_configs():
    """読み込み済み設定リスト取得"""
    # 自力実装

# Validation Tests (8件)
def test_api_config_base_url_validation():
    """base_url最小長さバリデーション"""
    # AI 30%協働

def test_api_config_timeout_validation():
    """timeout範囲バリデーション（0 < timeout <= 300）"""
    # 自力実装（base_url pattern踏襲）

def test_api_config_retry_count_validation():
    """retry_count範囲バリデーション（0 <= count <= 10）"""
    # 自力実装

def test_log_config_level_validation():
    """log level正規表現バリデーション"""
    # 自力実装

def test_log_config_format_validation():
    """log format正規表現バリデーション"""
    # 自力実装

def test_config_immutability():
    """frozen=True動作確認"""
    # AI 30%協働

def test_unknown_config_name():
    """未知の設定名エラー"""
    # 自力実装

def test_validation_error_handling():
    """ValidationError正常処理"""
    # 自力実装

# Error Handling Tests (5件)
def test_config_not_found_error():
    """ConfigNotFoundError発生確認"""
    # 自力実装

def test_config_validation_error():
    """ConfigValidationError発生確認"""
    # 自力実装

def test_logger_error_logging():
    """エラーログ出力確認」
    # AI 30%協働

def test_multiple_config_loading():
    """複数設定同時読み込み」
    # 自力実装

def test_config_override():
    """設定上書き動作」
    # 自力実装

# Integration Tests (7件)
def test_cross_config_validation_production_debug():
    """Production + DEBUG警告確認」
    # AI 40%協働

def test_cross_config_validation_high_timeout():
    """高タイムアウト + DEBUG警告確認」
    # 自力実装（production pattern踏襲）

def test_type_safe_shortcuts():
    """get_api_config/get_log_config型安全性」
    # 自力実装

def test_environment_enum_usage():
    """Environment Enum統合動作」
    # 自力実装

def test_structlog_integration():
    """structlogログ出力統合」
    # AI 30%協働

def test_config_manager_initialization():
    """初期化処理完全性」
    # 自力実装

def test_full_workflow():
    """load → validate → get 完全フロー」
    # 自力実装
```

**6. 品質ゲート実行 (20分)**
```bash
uv run pytest --cov=config/ --cov-fail-under=85
uv run ruff check config/
uv run mypy config/
```

**7. バッファ (10分)**

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度確認問題実施（25点満点、20点以上で合格）
- 学習記録更新（learning_state.yaml + daily_progress.md）

**復習バッファ (1h)**
- Pydantic deep dive（Field constraints詳細）
- BaseModel vs BaseSettings使い分け復習

**ポートフォリオ成果物**:
- [ ] Environment Enum実装（4値定義）
- [ ] APIConfigSchema実装（Field constraints 5項目）
- [ ] LogConfigSchema実装（pattern validation 2項目）
- [ ] ConfigManager class実装（200行）
  - [ ] load_config メソッド
  - [ ] get_config メソッド
  - [ ] get_api_config / get_log_config shortcuts
  - [ ] validate_cross_config_dependencies メソッド
  - [ ] list_loaded_configs メソッド
- [ ] ConfigValidationError / ConfigNotFoundError 例外定義
- [ ] 25テスト実装（Basic 5 + Validation 8 + Error 5 + Integration 7）
- [ ] 累計90テスト（+25テスト）
- [ ] カバレッジ85%維持
- [ ] 品質ゲート全合格（pytest/ruff/mypy）

**Day 28完了確認**:
- [ ] ConfigManager完全実装完了（200行）
- [ ] 25テスト全合格
- [ ] カバレッジ85%以上維持
- [ ] ConfigManager理解度85%以上
- [ ] 依存関係検証パターン理解85%以上

---

#### **Day 29 (金曜): APIClient統合 + Week 5完成**

**総学習時間**: 7時間
**カバレッジ目標**: 85%

**Phase 1: AI説明・概念理解 (2h)** ※短縮版 2.5h→2h（Day 28 ConfigManager理解により統合パターン理解時間削減）
- ConfigManager統合パターン理解（Day 28復習） **[30分]**
- BaseAPIClient/AsyncAPIClient Settings統合設計 **[50分]**
  - constructor引数 vs Settings自動読み込みの使い分け
  - デフォルト値フォールバック設計
- structlog設定統合パターン理解 **[30分]**
- 理解度測定（15分） **[15分]**
- バッファ **[5分]**

理解度チェックリスト:
  - [ ] ConfigManager統合の利点を説明できる
  - [ ] constructor引数とSettings自動読み込みの使い分けを理解している
  - [ ] デフォルト値フォールバックパターンを理解している
  - [ ] structlog設定統合の仕組みを理解している
  - [ ] 環境別動作（development/production）を理解している

判定基準:
  - 3つ以上✅ → Phase 2へ進む（理解度30%+達成）
  - 0-2つ✅ → AI再説明依頼 → 自己学習継続（理解度<30%）

**Phase 2: AI協働実装 (3.5h)** ※拡大版 2.5h→3.5h（30分 from Phase 1 + 30分 from Buffer）

**実装タスク**:

```python
# 1. BaseAPIClient Settings統合 (60分) - AI 65%
from config.settings import settings

class BaseAPIClient:
    def __init__(
        self,
        base_url: str | None = None,
        timeout: int | None = None,
        retry_count: int | None = None,
    ):
        """
        Settings統合（デフォルト値フォールバック）
        明示的引数 > Settings > ハードコードデフォルト
        """
        self.base_url = base_url or settings.api.base_url
        self.timeout = timeout or settings.api.timeout
        self.retry_count = retry_count or settings.api.retry_count
        self.retry_delay = settings.api.retry_delay
        # AI 65%協働 → デフォルト値パターン理解
```

```python
# 2. AsyncAPIClient Settings統合 (60分) - AI 65%
class AsyncAPIClient:
    def __init__(
        self,
        base_url: str | None = None,
        timeout: int | None = None,
    ):
        self.base_url = base_url or settings.api.base_url
        self.timeout = timeout or settings.api.timeout
        self.max_connections = settings.api.max_connections
        # AI 65%協働（BaseAPIClient pattern踏襲）
```

```python
# 3. structlog設定統合 (50分) - AI 70%
from config.settings import settings
import structlog

def setup_logging():
    """structlog設定統合"""
    level = settings.log.level
    format_type = settings.log.format

    processors = [
        structlog.stdlib.add_log_level,
        structlog.processors.TimeStamper(fmt="iso"),
    ]

    if format_type == "json":
        processors.append(structlog.processors.JSONRenderer())
    else:
        processors.append(structlog.dev.ConsoleRenderer())

    structlog.configure(
        processors=processors,
        wrapper_class=structlog.stdlib.BoundLogger,
        context_class=dict,
        logger_factory=structlog.stdlib.LoggerFactory(),
    )
    # AI 70%協働 → structlog統合理解
```

```python
# 4. 環境別テスト実装 (40分) - AI 70%
def test_development_settings():
    """Development環境設定確認"""
    # ENVIRONMENT=development
    assert settings.environment == "development"
    assert settings.debug is True
    # AI 70%協働

def test_production_settings():
    """Production環境設定確認"""
    # ENVIRONMENT=production
    assert settings.debug is False
    assert settings.log.level != "DEBUG"  # 本番環境DEBUG禁止
    # 自力実装（development pattern踏襲）

def test_api_client_settings_integration():
    """APIClient Settings統合確認"""
    client = BaseAPIClient()
    assert client.base_url == settings.api.base_url
    assert client.timeout == settings.api.timeout
    # 自力実装

def test_api_client_explicit_override():
    """明示的引数によるSettings上書き確認"""
    client = BaseAPIClient(base_url="https://override.example.com")
    assert client.base_url == "https://override.example.com"
    # 自力実装

def test_structlog_configuration():
    """structlog設定統合確認」
    setup_logging()
    logger = structlog.get_logger()
    # AI 65%協働
```

**5. 品質ゲート実行 (20分)**
```bash
uv run pytest --cov=. --cov-fail-under=85
uv run ruff check .
uv run mypy utils/ config/
```

**6. Week 5完了確認 (20分)**
- [ ] 累計テスト95件達成確認
- [ ] カバレッジ85%達成確認
- [ ] ConfigManager完全統合確認
- [ ] Week 5学習目標達成確認

**7. バッファ (10分)**

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度確認問題実施（25点満点、20点以上で合格）
- 学習記録更新（learning_state.yaml + daily_progress.md）

**復習バッファ (1h)**
- Week 5総復習（pytest fixture, Mock/Patch, Pydantic Settings, ConfigManager）
- 弱点補強

**ポートフォリオ成果物**:
- [ ] BaseAPIClient Settings統合
- [ ] AsyncAPIClient Settings統合
- [ ] JSONPlaceholderClient Settings統合（継承により自動対応）
- [ ] デフォルト値フォールバック実装（3パターン）
- [ ] ログ設定統合（structlog連携）
- [ ] setup_logging関数実装
- [ ] ログレベル動作確認（DEBUG/INFO/WARNING）
- [ ] 環境別テスト5件（development/production）
- [ ] 累計95テスト（+5テスト）
- [ ] カバレッジ85%達成
- [ ] 品質ゲート全合格（pytest/ruff/mypy）
- [ ] Week 5完了確認完了

**Week 5完了確認**:
- [ ] 95テスト達成（目標80超過）
- [ ] カバレッジ85%達成（目標78%超過）
- [ ] pytest fixture理解度85%以上
- [ ] Mock/Patch理解度85%以上
- [ ] Pydantic Settings理解度85%以上
- [ ] ConfigManager理解度85%以上
- [ ] Week 5全タスク完了

---

#### **
 (土曜): Week 5振り返り専念**

**総学習時間**: 7時間
**カバレッジ目標**:

**Phase 1: AI説明・概念理解 (2.5h)**
- pytest理解度確認（fixture, Mock/Patch） **[50分]**
- 品質ゲート最終確認（pytest/ruff/mypy全合格） **[50分]**
- Week 5総括・振り返り分析 **[40分]**
- 理解度測定（15分） **[15分]**
- バッファ **[15分]**

**Phase 2: AI協働実装 (3h)**
- Week 5振り返り作業（躓き箇所分析、理解度評価） **[100分]**
- 週次レポート作成（learning_state.yaml更新） **[60分]**
- バッファ **[20分]**

**Phase 3: 理解度確認・記録 (0.5h)**
- 理解度テスト（AI出題採点方式）
- 学習記録更新（learning_state.yaml, daily_progress.md）

**復習バッファ (1h)**
- Week 5総復習・弱点補強

---

```
Week 5振り返り
   - [ ] 80テスト達成確認
   - [ ] カバレッジ80%以上確認
   - [ ] 躓き箇所リストアップ
```

**Week 5学習完了確認**:
- [ ] pytest fixture理解度85%以上
- [ ] Mock/Patch理解度85%以上
- [ ] Pydantic Settings理解度85%以上

**ポートフォリオ成果物**:
- [ ] fixture実装（session/module/function scope）
- [ ] Mock/Patch実装（httpx, ファイルシステム）
- [ ] Pydantic Settings統合
- [ ] 週次レポート完成
- [ ] 全品質ゲート合格（pytest/ruff/mypy）

---

## Phase 2: DevOps Integration (Week 6-7, 102時間)

### Week 6: 統合復習 + 自律チャレンジ (42時間)

**学習目標**:
- Week 1-6全概念復習・弱点補強
- 自律チャレンジ合格（新API統合実装）
- Phase 1完了確認
- 自律達成率60%以上

**学習方針**: 自律チャレンジ（AI使用なし）
**認知負荷**: 60%

**学習フロー**: @[CLAUDE.md#AI協働学習フロー仕組み]
- Phase 1: AI説明・概念理解（目標理解度30%）
- Phase 2: AI協働実装（目標理解度60%）
- Phase 3: 理解度確認・記録（目標理解度70%+）

---

##### Day 31（月）: Python + httpx復習

**復習内容**:
```python
# Week 1-2復習 (AI 40%)
1. 概念整理 (2.5h)
   - httpxエラー階層
   - リトライロジック
   - Context Manager
   # AI: 概念図作成支援

2. 弱点補強 (2.5h)
   - 理解不足箇所再学習
   - AI支援最小限

3. ミニ課題 (1h)
   - 簡単なAPI client作成
   - AI使用50%以下
```

**ポートフォリオ成果物**:
- [ ] Week 1-2概念図完成
- [ ] 弱点リスト解消
- [ ] ミニ課題完成

---

##### Day 32（火）: Async + Error Handling復習

**復習内容**:
```python
# Week 3-4復習 (AI 40%)
1. Async概念整理 (2.5h)
   - asyncio動作原理
   - gather効果
   - AI: 図解作成

2. 弱点補強 (2.5h)
   - 非同期エラー処理
   - 並行処理パターン

3. ミニ課題 (1h)
   - 非同期API実装
   - AI使用50%以下
```

**ポートフォリオ成果物**: (同様)

---

##### Day 33: Testing + Settings復習

**復習内容**:
```python
# Week 5-6復習 (AI 40%)
1. pytest概念整理 (2.5h)
   - Fixture scope
   - Mock/Patch
   - Settings構造

2. 弱点補強 (2.5h)
   - テスト設計パターン

3. ミニ課題 (1h)
   - 複雑なfixture作成
   - AI使用50%以下
```

**復習完了確認**:
- [ ] Week 1-6全概念図完成
- [ ] 弱点リスト全解消
- [ ] ミニ課題3個完成
- [ ] 自律率55%以上

---

**🚨 最重要: Phase 1合否判定**

##### Day 34-36: 統合自律チャレンジ

**総学習時間**: 20時間 (3日間)

**課題**: **新API統合実装**
```python
# 課題: GitHub API or OpenWeather API統合
要件:
1. Sync + Async両対応API client実装
2. エラー階層設計
3. pytest fixture作成
4. 30テスト作成 (カバレッジ70%+)
5. Pydantic Settings統合
6. README作成

制約:
- AI使用完全禁止 (Claude, ChatGPT, Copilot等)
- 公式ドキュメント閲覧OK
- Stack Overflow閲覧OK
- 既存コード参照OK
- 時間: 20時間 (3日間)
```

**評価基準**:
```
完成度 (50%):
- [ ] Sync client実装 (15点)
- [ ] Async client実装 (15点)
- [ ] エラーハンドリング (10点)
- [ ] Settings統合 (10点)

品質 (30%):
- [ ] 30テスト実装 (15点)
- [ ] カバレッジ70%+ (10点)
- [ ] ruff/mypy合格 (5点)

ドキュメント (10%):
- [ ] README作成 (10点)

自律度 (10%):
- [ ] AI参照0回 (10点)
- [ ] 1-3回 (-5点)
- [ ] 4回以上 (0点)

**合格: 75点以上**
```

**Day 22 (6時間)**: API調査 + 基本実装
```
09:00-12:00: API仕様理解・設計
13:00-16:00: Sync client実装開始
```

**Day 23 (8時間)**: Async実装 + テスト
```
09:00-12:00: Async client実装
13:00-16:00: エラーハンドリング
16:00-19:00: テスト作成 (15テスト)
```

**Day 24 (6時間)**: 仕上げ + テスト完成
```
09:00-12:00: テスト完成 (30テスト)
13:00-16:00: README + 最終確認
```

---

##### **合否判定**

**合格 (75点以上)**:
- Phase 2 (Week 8) 進行
- 自律達成率60%認定
- Docker + CI/CD学習開始

**不合格 (74点以下)**:
- +1週復習 (Week 7延長)
- 弱点分野集中補強
- 再チャレンジ (1回のみ)

**2回連続不合格**:
- 10週プランに延長
- Week 1-6から弱点再学習

---

### Week 7: Docker基盤構築 (42時間)

**学習目標**:
- Docker 4-stage Dockerfile実装（Base → Dependencies → Runtime → Test）
- docker-compose.yml 4環境構築（dev/test/demo/prod）
- Alpine Linux最適化（イメージサイズ削減）
- カバレッジ70%達成（Week 7終了時目標）

**認知負荷**: 70%
**自律目標**: Docker設計理解40%、マルチステージ理解35%

**学習フロー**: @[CLAUDE.md#AI協働学習フロー仕組み]
- Phase 1: AI説明・概念理解（目標理解度30%）
- Phase 2: AI協働実装（目標理解度60%）
- Phase 3: 理解度確認・記録（目標理解度70%+）

---

#### **Day 37 (月曜): Docker基礎 + Multi-stage概念理解**

**総学習時間**: 7時間
**カバレッジ目標**: 63%（累積）

**学習内容**:
```bash
# Docker基礎 (AI 85%)
1. Docker概念理解 (1h)
   - イメージとコンテナ
   - レイヤー構造
   - AI: 図解作成

2. 基本コマンド (1h)
   docker run, exec, ps, logs
   # AI: サンプル実行

3. 実際に使用 (1h)
   docker run python:3.12-slim
   docker exec -it <container> bash
```

```dockerfile
# Dockerfile基本 (AI 85%)
FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["pytest"]

# AI生成 → 各行意味理解
```

```dockerfile
# 2-stage Dockerfile (AI 80%)
# Stage 1: Builder
FROM python:3.12-slim AS builder
WORKDIR /app
COPY requirements.txt .
RUN pip install --user -r requirements.txt

# Stage 2: Runtime
FROM python:3.12-slim
WORKDIR /app
COPY --from=builder /root/.local /root/.local
COPY . .

# AI実装 → 2-stage効果理解
```

**ポートフォリオ成果物**:
- [ ] Docker基本理解30%
- [ ] 基本コマンド実行成功
- [ ] 基本Dockerfile作成
- [ ] docker build成功
- [ ] docker run成功
- [ ] 2-stage Dockerfile作成
- [ ] イメージサイズ比較
- [ ] 2-stage効果理解

---

#### **Day 38 (火曜): docker-compose**

**総学習時間**: 10時間

**学習内容**:
```yaml
# docker-compose.yml (AI 80%)
version: "3.8"

services:
  app:
    build: .
    volumes:
      - .:/app
    command: pytest

# AI生成 → サービス定義理解
```

```yaml
# docker-compose.yml (AI 80%)
services:
  dev:
    build: .
    volumes:
      - .:/app
    command: bash

  test:
    build: .
    command: pytest -v

# AI実装 → 環境分離理解
```

```dockerfile
# 最適化 (AI 75%)
# .dockerignore作成
# レイヤーキャッシュ活用
# AI: 最適化テクニック実装
```

**ポートフォリオ成果物**:
- [ ] docker-compose.yml作成
- [ ] docker-compose up成功
- [ ] dev/test環境分離
- [ ] docker-compose run dev成功
- [ ] docker-compose run test成功
- [ ] .dockerignore作成
- [ ] ビルド時間短縮確認
- [ ] イメージサイズ最適化

---

#### **Day 39 (水曜): GitHub Actions基礎 + 中間確認**

**総学習時間**: 10時間

**学習内容**:
```yaml
# .github/workflows/test.yml (AI 85%)
name: Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: pytest

# AI生成 → workflow理解
```

```yaml
# 統合workflow (AI 85%)
jobs:
  test:
    # テスト実行

  quality:
    needs: test
    # ruff, mypy実行

  coverage:
    needs: test
    # カバレッジ測定

# AI実装 → job依存理解
```

**ポートフォリオ成果物**:
- [ ] test.yml作成
- [ ] GitHub push
- [ ] workflow実行成功
- [ ] 統合workflow完成
- [ ] 全job成功
- [ ] 累計100テスト達成

---

#### **Day 40 (木曜): CI/CD最適化**

**総学習時間**: 10時間

**学習内容**:
```yaml
# キャッシュ設定 (AI 85%)
- uses: actions/cache@v3
  with:
    path: ~/.cache/pip
    key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}

# AI実装 → キャッシュ効果理解
```

```markdown
# README.mdバッジ追加 (AI 80%)
![Test](https://github.com/.../workflows/test/badge.svg)
![Coverage](https://img.shields.io/codecov/c/github/...)
```

```yaml
# Security scan (AI 85%)
- run: pip install safety bandit
- run: safety check
- run: bandit -r utils/ config/
```

**ポートフォリオ成果物**:
- [ ] キャッシュ設定
- [ ] ビルド時間短縮確認
- [ ] バッジ追加
- [ ] status表示確認
- [ ] セキュリティスキャン追加
- [ ] スキャン合格

---

#### **Day 41 (金曜): Week 7仕上げ**

**総学習時間**: 10時間

**学習内容**:
```markdown
# README最終更新 (AI 75%)
## Docker使用方法
## CI/CD説明
## バッジ追加
```

**ポートフォリオ成果物**:
- [ ] README完全版完成
- [ ] Architecture diagram追加
- [ ] 累計100テスト達成
- [ ] カバレッジ80%以上

**完了確認項目**:
- [ ] Dockerfile (2-stage)
- [ ] docker-compose.yml
- [ ] GitHub Actions統合workflow
- [ ] 100テスト
- [ ] カバレッジ80%+
- [ ] README完全版

---

#### **Day 42 (土曜): Week 7振り返り + Phase 2完了レポート**

**総学習時間**: 10時間

**Phase 2完了条件**:
- [ ] Week 7自律チャレンジ合格 (75点以上)
- [ ] 100テスト達成
- [ ] カバレッジ80%以上
- [ ] Docker + CI/CD稼働
- [ ] 自律達成率55%以上
- [ ] ポートフォリオ完成度80%以上

**Week 9-10計画** (Optional)**:
- [ ] Phase 2レポート完成
- [ ] 8週完了判定完了
- [ ] Phase 3計画決定
- [ ] 日曜完全休息準備

---

### Week 8: ポートフォリオ最適化 (60時間)

**学習目標**:
- README完全版作成
- Case Study作成
- GitHub活動最適化
- ドキュメント品質90%達成

**認知負荷**: 65%

**学習フロー**: @[CLAUDE.md#AI協働学習フロー仕組み]
- Phase 1: AI説明・概念理解（目標理解度30%）
- Phase 2: AI協働実装（目標理解度60%）
- Phase 3: 理解度確認・記録（目標理解度70%+）

---

#### **Day 43-45 (月-水曜): README最適化 (30時間)**

**総学習時間**: 各日10時間

##### README完全版作成

**学習内容**:
```markdown
# README.md完全版 (AI 70%)

## プロジェクト概要
- 問題提起
- 解決方法
- 技術スタック

## アーキテクチャ
- システム構成図 (mermaid)
- コンポーネント説明
- 技術選定理由

## 機能
- 機能一覧
- デモGIF/画像
- コードスニペット

## インストール・使用方法
- 詳細手順
- トラブルシューティング

## テスト・品質
- テスト戦略
- カバレッジ
- CI/CD説明

## パフォーマンス
- 同期 vs 非同期比較
- パフォーマンス測定結果

## セキュリティ
- セキュリティ対策
- 脆弱性対応

## 今後の展望
- 改善計画
- 学習継続項目

# AI生成 → 調整・磨き上げ30%
```

**ポートフォリオ成果物**:
- [ ] README完全版完成
- [ ] 英語版作成
- [ ] 画像・図追加

---

#### **Day 46-48 (木-土曜): Case Study + GitHub活動 (30時間)**

##### Case Study作成

**学習内容**:
```markdown
# CASE_STUDY.md作成 (AI 60%)

## 背景・課題
- なぜこのプロジェクトを作ったか
- 解決したい課題

## 技術選定
- なぜPython + httpx + pytestか
- 技術選定理由

## 実装の工夫
- 設計パターン
- パフォーマンス最適化
- テスト戦略

## 学んだこと
- 技術的学び
- プロセス改善

## 成果
- 定量的成果
- 技術力向上
```

**ポートフォリオ成果物**:
- [ ] Case Study完成
- [ ] Architecture diagram追加
- [ ] 技術ブログ執筆 (Optional)

---

### Week 9: 応募準備 + 初回応募 (60時間)

**学習目標**:
- プラットフォーム登録 (Levtech, Findy)
- 応募文面作成 (10件分)
- 面談想定Q&A作成
- 初回応募実行 (5-10件)

**認知負荷**: 65%

**学習フロー**: @[CLAUDE.md#AI協働学習フロー仕組み]
- Phase 1: AI説明・概念理解（目標理解度30%）
- Phase 2: AI協働実装（目標理解度60%）
- Phase 3: 理解度確認・記録（目標理解度70%+）

---

#### **Day 49-51 (月-水曜): プラットフォーム登録 + プロフィール作成 (30時間)**

##### プロフィール作成

**学習内容**:
```
1. Levtech登録 (AI 70%)
   - プロフィール作成
   - スキル登録
   - ポートフォリオリンク

2. Findy登録 (AI 70%)
   - 同様

3. プロフィール最適化 (AI 60%)
   - GitHub連携
   - スキル証明
```

**ポートフォリオ成果物**:
- [ ] Levtech登録完了
- [ ] Findy登録完了
- [ ] プロフィール完成

---

#### **Day 52-54 (木-土曜): 応募準備 + 初回応募 (30時間)**

##### 応募文面作成

**学習内容**:
```markdown
# 応募文面テンプレート (AI 70%)

件名: Python + API統合開発案件応募

本文:
はじめまして、[名前]と申します。

【自己紹介】
- Python APIテスト + DevOps経験
- GitHub: [URL]
- 技術スタック: Python, httpx, pytest, Docker, CI/CD

【ポートフォリオ】
- JSONPlaceholder API統合プロジェクト
- 100テスト (カバレッジ80%)
- GitHub Actions CI/CD稼働
- 詳細: [GitHub URL]

【対応可能業務】
- REST API統合開発
- pytest テスト作成
- Docker環境構築
- CI/CD設定

【稼働可能時間】
- 週16-24時間
- 平日夜間・土日対応可

何卒よろしくお願いいたします。

# AI生成 → 調整20%
```

**面談想定Q&A作成**:
```markdown
# 面談想定Q&A (AI 60%)

Q1: なぜこのプロジェクトを作成したのですか?
A: API統合開発の実務スキル証明のため...

Q2: 最も苦労した点は?
A: 非同期エラーハンドリング設計...

Q3: テストカバレッジ80%達成方法は?
A: pytest fixture活用...

# 30問作成
```

**ポートフォリオ成果物**:
- [ ] 応募文面10件作成
- [ ] 面談Q&A 30問作成
- [ ] 初回応募5-10件実行

---

## 毎週の共通ルーティン

### 月曜 (計画)

**09:00-09:30 (30分)**:
```
週次計画ミーティング (自分と):
1. 今週の学習目標設定
   - [ ] 主要成果物
   - [ ] テスト目標

2. GitHub Issue作成
   - [ ] Week X Tasks
   - [ ] 各Day成果物リスト

3. AI協働トラッキング準備
   - [ ] 測定シート準備
```

---

### 水曜 (中間確認)

**13:00-14:00 (1時間)**:
```
中間確認:
1. 進捗確認
   - [ ] 50%到達確認
   - [ ] 成果物達成状況

2. 躓き箇所洗い出し
   - 理解不足箇所: ______
   - 時間不足箇所: ______
```

---

### 金曜 (AI-Free Challenge + 振り返り)

**16:00-18:00 (2時間): AI-Free Challenge**
```
🚨 AI完全禁止
課題実施 (Week別課題)
評価・記録
```

**18:00-19:00 (1時間): 週次振り返り**
```
週次学習レポート作成:
1. 達成事項確認
   - [ ] 成果物リスト
   - [ ] テスト数・カバレッジ

2. 学習効率評価
   - 認知負荷: ___%
   - 燃え尽け兆候: 有/無
   - 理解深度: ___%
   - 次週調整: ______
```

---

### 日曜 (完全休息)

**🚨 重要: 学習完全停止**
```
- [ ] 学習関連作業一切禁止
- [ ] リフレッシュ活動
- [ ] 燃え尽き防止
- [ ] 次週への充電
```

---

## AI-Free Challenge評価基準詳細

### Week 1-2 Challenge

**課題**: `get_albums(user_id)` 実装
**目標時間**: 60分
**合格基準**: 60点以上

**評価項目**:
```
完成度 (40点):
- get_albumsメソッド実装 (20点)
  - エンドポイント正しい: 10点
  - 引数処理正しい: 5点
  - return正しい: 5点
- エラーハンドリング (10点)
  - 例外処理実装: 5点
  - 適切な例外使用: 5点
- 3テスト実装 (10点)
  - success test: 4点
  - filter test: 3点
  - error test: 3点

品質 (30点):
- ruff合格 (15点)
  - エラー0件: 15点
  - エラー1-3件: 10点
  - エラー4件以上: 0点
- テスト合格 (15点)
  - 3テスト全合格: 15点
  - 2テスト合格: 10点
  - 1テスト以下: 0点

速度 (20点):
- 60分以内完了: 20点
- 60-90分: 10点
- 90分超: 0点

自律度 (10点):
- AI参照0回: 10点
- 1-2回: 5点
- 3回以上: 0点

合格: 60点以上
```

---

### Week 3-4 Challenge

**課題**: Async `get_comments(post_id)` 実装
**目標時間**: 90分
**合格基準**: 65点以上

**評価項目**:
```
完成度 (40点):
- async get_commentsメソッド実装 (20点)
  - エンドポイント正しい (/posts/{post_id}/comments): 8点
  - 引数処理正しい (post_id: int): 5点
  - async/await正しく使用: 5点
  - return正しい (list[dict]): 2点
- 非同期エラーハンドリング (10点)
  - async try/except実装: 5点
  - APITimeoutError, APIHTTPError使用: 5点
- 3テスト実装 (10点)
  - async success test: 4点
  - async error test: 3点
  - async performance test: 3点

品質 (30点):
- pytest-asyncio正しく使用 (10点)
  - @pytest.mark.asyncio使用: 5点
  - async test関数定義: 5点
- ruff合格 (10点)
  - エラー0件: 10点
  - エラー1-3件: 7点
  - エラー4件以上: 0点
- テスト合格 (10点)
  - 3テスト全合格: 10点
  - 2テスト合格: 7点
  - 1テスト以下: 0点

速度 (20点):
- 90分以内完了: 20点
- 90-120分: 10点
- 120分超: 0点

自律度 (10点):
- AI参照0回: 10点
- 1-2回: 5点
- 3回以上: 0点

合格: 65点以上
```

---

### Week 5-6 Challenge

**課題**: 新fixture + parametrized test実装
**目標時間**: 90分
**合格基準**: 70点以上

**評価項目**:
```
完成度 (40点):
- 新fixtureの実装 (20点)
  - async fixture作成: 8点
  - 適切なscope設定: 5点
  - factory pattern使用: 5点
  - 依存関係正しく設定: 2点
- parametrized testの実装 (10点)
  - @pytest.mark.parametrize使用: 5点
  - 複数テストケース設定: 3点
  - 適切なテストデータ設計: 2点
- 3テスト実装 (10点)
  - fixture使用test: 4点
  - parametrized test: 3点
  - 統合test: 3点

品質 (30点):
- fixture品質 (15点)
  - DRY原則遵守: 5点
  - 再利用性高い: 5点
  - docstring記載: 5点
- ruff合格 (10点)
  - エラー0件: 10点
  - エラー1-3件: 7点
  - エラー4件以上: 0点
- テスト合格 (5点)
  - 3テスト全合格: 5点
  - 2テスト合格: 3点
  - 1テスト以下: 0点

速度 (20点):
- 90分以内完了: 20点
- 90-120分: 10点
- 120分超: 0点

自律度 (10点):
- AI参照0回: 10点
- 1-2回: 5点
- 3回以上: 0点

合格: 70点以上
```

---

### Week 7 Challenge (最重要)

**課題**: 新API統合実装
**時間**: 20時間 (3日間)
**合格基準**: 75点以上

**評価項目**:
```
完成度 (50点):
- Sync client実装 (15点)
- Async client実装 (15点)
- エラーハンドリング (10点)
- Settings統合 (10点)

品質 (30点):
- 30テスト実装 (15点)
- カバレッジ70%+ (10点)
- ruff/mypy合格 (5点)

ドキュメント (10点):
- README作成 (10点)

自律度 (10点):
- AI参照0回 (10点)
- 1-3回 (-5点)
- 4回以上 (0点)

合格: 75点以上
```

---

## 成功指標 (KPI) 詳細

### Phase 1完了時 (Week 6)

**必須達成項目**:
- [ ] 80テスト (カバレッジ78%+)
- [ ] ruff + mypy合格
- [ ] pre-commit稼働
- [ ] 自律達成率50%以上
- [ ] AI-Free Challenge 6回合格
- [ ] 認知負荷75%以内維持

**技術理解度**:
- Python実装力: 60%以上
- httpx理解: 65%以上
- Async理解: 45%以上
- pytest能力: 55%以上
- Settings理解: 60%以上

---

### Phase 2完了時 (Week 8)

**必須達成項目**:
- [ ] Week 7自律チャレンジ合格 (75点以上)
- [ ] Docker + CI/CD稼働
- [ ] 100テスト (カバレッジ80%+)
- [ ] GitHub Actions全チェック合格
- [ ] 自律達成率55%以上
- [ ] ポートフォリオ完成度80%以上

**技術理解度**:
- Docker理解: 40%以上
- CI/CD理解: 35%以上
- DevOps全体: 35%以上

**市場準備度**:
- 初回案件対応力: 65%以上
- 時給3,500-4,000円相当

---

### Phase 3完了時 (Week 10) ※Optional

**必須達成項目**:
- [ ] プロフェッショナルREADME完成
- [ ] Case Study作成
- [ ] プラットフォーム登録完了 (2社)
- [ ] 応募文面10件作成
- [ ] 初回応募5-10件実行
- [ ] 面談Q&A 30問準備
- [ ] 書類選考通過1件以上 (目標)

**市場準備度**:
- 応募準備度: 90%以上
- 面談準備度: 80%以上
- 自律達成率: 60%以上

---

## リスク管理詳細

### High Risk 1: Week 7自律チャレンジ不合格

**発生確率**: 40%
**影響度**: High (Phase 2開始不可)

**予防策**:
1. Week 1-6で毎週AI-Free Challenge実施
2. 自律達成率週次トラッキング
3. 不合格兆候 (3週連続65%未満) で早期復習

**発生時対応**:
1. +1週復習 (Week 8延期)
2. 弱点分野集中補強
3. 再チャレンジ (1回のみ)

**撤退基準**:
- 2回連続不合格 → 10週プランに延長
- 3回連続不合格 → 12週プランに延長

---

### High Risk 2: 認知負荷過剰 (燃え尽き)

**発生確率**: 40%
**影響度**: High (学習停滞)

**警告サイン**:
- 集中力持続30分以下
- 睡眠時間6h未満が3日連続
- 学習モチベーション著しい低下
- 週次認知負荷85%以上

**予防策**:
1. 日曜完全休息必須
2. 認知負荷週次モニタリング
3. 1日10h → 8h調整可能設定

**発生時対応**:
1. 即座に1日8h学習に調整
2. 認知負荷80%以内に管理
3. 必要に応じて12週プランに延長

---

### Medium Risk 3: 初回案件獲得失敗

**発生確率**: 30%
**影響度**: Medium (収益化遅延)

**予防策**:
1. 並行応募 (Levtech 5件, Findy 5件, Upwork 3件)
2. 単価柔軟 (2,500円案件も受諾可)
3. 応募文面AI支援で最適化

**発生時対応**:
1. Week 11-12で技術補強
2. ポートフォリオ強化
3. 応募件数増加 (20件)

**撤退基準**:
- Week 12まで0件受注 → プラン見直し

---

## 投資対効果 (ROI) 詳細

### 8週プラン ROI

**投資**:
- 学習時間: 480時間
- 学習期間: 8週間
- 応募期間: 3週間

**収益シミュレーション**:
```
Week 1-8:   学習 (収入0円)
Week 9-11:  応募 (収入0円)
Week 12:    初回案件 (3,500円 × 16h/週 = 56,000円)

Month 3 (Week 12-15): 224,000円
Month 4 (Week 16-19): 224,000円
Month 5 (Week 20-23): 280,000円 (単価UP 4,000円)
Month 6 (Week 24-27): 280,000円

6ヶ月累計: 1,008,000円
時間投資: 480時間
時給換算: 2,100円/h (学習時間含む)
```

**損益分岐点**: 学習開始から17週後

---

### 10週プラン ROI

**投資**:
- 学習時間: 600時間
- 学習期間: 10週間
- 応募期間: 3週間

**収益シミュレーション**:
```
Week 1-10:  学習 (収入0円)
Week 11-13: 応募 (収入0円)
Week 14:    初回案件 (3,500円 × 16h/週 = 56,000円)

Month 3.5 (Week 14-15): 112,000円
Month 4 (Week 16-19): 224,000円
Month 5 (Week 20-23): 280,000円 (単価UP 4,000円)
Month 6 (Week 24-27): 280,000円

6ヶ月累計: 896,000円
時間投資: 600時間
時給換算: 1,493円/h (学習時間含む)
```

**損益分岐点**: 学習開始から19週後

---

## 最終確認チェックリスト

### 8週完了判定

**全条件満たす必要**:
- [ ] Week 7自律チャレンジ合格 (75点以上)
- [ ] 100テスト達成
- [ ] カバレッジ80%以上
- [ ] Docker + CI/CD稼働
- [ ] 自律達成率55%以上
- [ ] ポートフォリオ完成度80%以上
- [ ] ruff/mypy合格
- [ ] GitHub Actions全チェック合格

**→ 全条件満たす: Week 9応募開始可能**
**→ 条件不足: Week 9-10で補強**

---

### 10週完了判定

**全条件満たす必要**:
- [ ] 8週完了条件全て
- [ ] README完全版完成
- [ ] Case Study作成
- [ ] プラットフォーム登録完了 (2社)
- [ ] 応募文面10件作成
- [ ] 面談Q&A 30問準備
- [ ] 自律達成率60%以上

**→ 全条件満たす: 初回案件獲得見込み高**

---

## まとめ

### プラン選択ガイド

**8週アグレッシブ (推奨条件)**:
- 1日7時間学習可能
- 認知負荷管理できる自信
- 早期市場投入優先
- リスク許容度高

**10週バランス (標準推奨)**:
- 1日7時間学習可能
- 安定した学習継続重視
- 成功確率80%優先
- バランス重視

**12週安全版 (保守的)**:
- 確実な成功優先
- リスク最小化
- 品質重視

### 成功の鍵

1. **AI-Free Challenge完遂** (毎週金曜)
2. **認知負荷管理** (週次モニタリング)
3. **日曜完全休息** (燃え尽き防止)
4. **Week 7自律チャレンジ合格** (最重要)

### 開始日・目標日

**開始日**: 2025年10月1日 (本日)
**8週完了**: 2025年11月26日
**10週完了**: 2025年12月10日
**初回案件**: 2025年12月中旬〜

---

**最終推奨**: 10週バランス型プランで開始、Week 7結果に応じて8週/10週/12週に調整

**今日から開始**: Day 1 (Python基礎復習 + httpx導入) → 10時間学習スタート
