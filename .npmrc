# npm script を bash で実行する（既定の sh=dash では set -o pipefail が使えないため）。
# lint:* スクリプトは `git ls-files | xargs` のパイプ構成で、pipefail が無いと
# git 側の失敗が xargs の正常終了に隠れて exit 0 になり、lint ゲートが無音で無効化される。
script-shell=bash
